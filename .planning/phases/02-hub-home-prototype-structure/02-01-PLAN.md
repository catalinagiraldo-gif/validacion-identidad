---
phase: 02-hub-home-prototype-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/models/prototype-meta.model.ts
  - src/app/pages/catalog/meta.json
  - src/app/pages/caza-productos/meta.json
  - src/app/pages/proveedores/meta.json
  - src/app/pages/orders-manual/meta.json
  - src/app/pages/mis-pedidos/meta.json
  - src/app/pages/orders-provider/meta.json
  - src/app/pages/historial-cartera/meta.json
  - src/app/pages/dropicard/meta.json
  - src/app/pages/cas/meta.json
  - src/app/pages/academy/meta.json
autonomous: true
requirements:
  - STRUC-01
  - STRUC-02
  - STRUC-03

must_haves:
  truths:
    - "A PrototypeMeta TypeScript interface exists with all required fields"
    - "Each of the 10 existing prototypes has a valid meta.json file next to its component"
    - "meta.json files contain accurate data for each prototype (title, module, route, profiles, owner, dateAdded, description)"
    - "No existing prototype route, import, or component file was modified"
  artifacts:
    - path: "src/app/models/prototype-meta.model.ts"
      provides: "PrototypeMeta interface definition"
      exports: ["PrototypeMeta"]
    - path: "src/app/pages/catalog/meta.json"
      provides: "Catalog prototype metadata"
      contains: "productos/catalogo"
    - path: "src/app/pages/caza-productos/meta.json"
      provides: "Caza Productos prototype metadata"
      contains: "productos/caza-productos"
    - path: "src/app/pages/proveedores/meta.json"
      provides: "Proveedores prototype metadata"
      contains: "productos/proveedores"
    - path: "src/app/pages/orders-manual/meta.json"
      provides: "Orden Manual prototype metadata"
      contains: "pedidos/orden-manual"
    - path: "src/app/pages/mis-pedidos/meta.json"
      provides: "Mis Pedidos prototype metadata"
      contains: "mis-pedidos/mis-pedidos"
    - path: "src/app/pages/orders-provider/meta.json"
      provides: "Orders Provider prototype metadata"
      contains: "pedidos/mis-pedidos-proveedor"
    - path: "src/app/pages/historial-cartera/meta.json"
      provides: "Historial Cartera prototype metadata"
      contains: "historial-de-cartera"
    - path: "src/app/pages/dropicard/meta.json"
      provides: "Dropicard prototype metadata"
      contains: "dropi-card/cards"
    - path: "src/app/pages/cas/meta.json"
      provides: "CAS Bandeja prototype metadata"
      contains: "cas/bandeja"
    - path: "src/app/pages/academy/meta.json"
      provides: "Academy prototype metadata"
      contains: "academy"
  key_links:
    - from: "meta.json files"
      to: "PrototypeMeta interface"
      via: "schema conformance"
      pattern: "title.*module.*slug.*route.*profiles.*owner.*dateAdded"
---

<objective>
Define the PrototypeMeta interface and create meta.json files for all 10 existing prototypes, placing each file next to its prototype component (NO migration).

Purpose: Establish the metadata schema and data foundation that the registry script (Plan 02) and Hub UI (Plan 03) will consume. Per D-03 and D-05, meta.json files are created IN-PLACE -- no file moves, no route changes, no import changes.

Output: 1 TypeScript interface file + 10 meta.json files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hub-home-prototype-structure/02-CONTEXT.md

Source files for prototype routes:
@src/app/app.routes.ts
@navigation-map.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PrototypeMeta interface</name>
  <files>src/app/models/prototype-meta.model.ts</files>
  <read_first>
    - src/app/models/user.model.ts (existing model pattern)
    - src/app/services/profile.service.ts (HubProfile type to reference)
  </read_first>
  <action>
Create `src/app/models/prototype-meta.model.ts` with the following exported interface (per D-09 through D-13):

```typescript
export interface PrototypeMeta {
  title: string;          // Display name, e.g. "Catalogo de Productos"
  module: string;         // Module name, e.g. "productos"
  slug: string;           // Unique identifier, e.g. "catalogo"
  route: string;          // Full route path, e.g. "/productos/catalogo"
  description: string;    // Short description for tooltip hover (per D-07, D-13)
  profiles: string[];     // Array of role strings: "dropshipper" | "proveedor" | "admin" (per D-09)
  owner: string;          // Email @dropi.co, e.g. "producto@dropi.co" (per D-10)
  dateAdded: string;      // ISO date string, e.g. "2026-05-05" (per D-11)
  thumbnail: string;      // Relative path to screenshot, e.g. "thumbnail.png" (per D-12)
}
```

Do NOT import HubProfile type for the profiles field -- use `string[]` to keep meta.json-compatible (JSON has no type unions). The registry script will validate at build time.
  </action>
  <verify>
    <automated>grep -c "export interface PrototypeMeta" src/app/models/prototype-meta.model.ts | grep -q "1" && grep -c "profiles: string\[\]" src/app/models/prototype-meta.model.ts | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/app/models/prototype-meta.model.ts
    - Contains `export interface PrototypeMeta`
    - Has exactly these 9 fields: title, module, slug, route, description, profiles, owner, dateAdded, thumbnail
    - `profiles` is typed as `string[]` (not single string)
    - `owner` is typed as `string`
    - `dateAdded` is typed as `string`
    - `thumbnail` is typed as `string`
  </acceptance_criteria>
  <done>PrototypeMeta interface exported with all 9 fields matching the D-09 through D-13 schema decisions</done>
</task>

<task type="auto">
  <name>Task 2: Create meta.json for all 10 prototypes</name>
  <files>
    src/app/pages/catalog/meta.json
    src/app/pages/caza-productos/meta.json
    src/app/pages/proveedores/meta.json
    src/app/pages/orders-manual/meta.json
    src/app/pages/mis-pedidos/meta.json
    src/app/pages/orders-provider/meta.json
    src/app/pages/historial-cartera/meta.json
    src/app/pages/dropicard/meta.json
    src/app/pages/cas/meta.json
    src/app/pages/academy/meta.json
  </files>
  <read_first>
    - src/app/app.routes.ts (exact routes for each prototype)
    - navigation-map.json (roles, descriptions, figmaSource for each prototype)
    - src/app/models/prototype-meta.model.ts (interface just created in Task 1)
  </read_first>
  <action>
Create a `meta.json` file NEXT TO each existing prototype component (per D-03). Each meta.json conforms to the PrototypeMeta interface. Use `"thumbnail": "thumbnail.png"` for all (files will be generated by Playwright in Plan 04).

The exact data for each meta.json (derived from navigation-map.json and app.routes.ts):

**1. src/app/pages/catalog/meta.json**
```json
{
  "title": "Catalogo de Productos",
  "module": "productos",
  "slug": "catalogo",
  "route": "/productos/catalogo",
  "description": "Catalogo de productos con busqueda semantica IA, filtros avanzados, carousel de proveedores y grid de product cards.",
  "profiles": ["dropshipper"],
  "owner": "producto@dropi.co",
  "dateAdded": "2026-05-01",
  "thumbnail": "thumbnail.png"
}
```

**2. src/app/pages/caza-productos/meta.json**
```json
{
  "title": "Caza Productos",
  "module": "productos",
  "slug": "caza-productos",
  "route": "/productos/caza-productos",
  "description": "Herramienta para descubrir productos trending con filtros por categoria, estado y ofertas.",
  "profiles": ["dropshipper"],
  "owner": "producto@dropi.co",
  "dateAdded": "2026-05-01",
  "thumbnail": "thumbnail.png"
}
```

**3. src/app/pages/proveedores/meta.json**
```json
{
  "title": "Proveedores",
  "module": "productos",
  "slug": "proveedores",
  "route": "/productos/proveedores",
  "description": "Listado de proveedores disponibles con grid de cards, filtros por tipo, ciudad y categorias.",
  "profiles": ["dropshipper"],
  "owner": "producto@dropi.co",
  "dateAdded": "2026-05-01",
  "thumbnail": "thumbnail.png"
}
```

**4. src/app/pages/orders-manual/meta.json**
```json
{
  "title": "Orden Manual",
  "module": "pedidos",
  "slug": "orden-manual",
  "route": "/pedidos/orden-manual",
  "description": "Modal para crear una orden manual con datos del cliente, seleccion de productos, transportadora y resumen con ganancia.",
  "profiles": ["dropshipper"],
  "owner": "producto@dropi.co",
  "dateAdded": "2026-05-01",
  "thumbnail": "thumbnail.png"
}
```

**5. src/app/pages/mis-pedidos/meta.json**
```json
{
  "title": "Mis Pedidos",
  "module": "pedidos",
  "slug": "mis-pedidos",
  "route": "/mis-pedidos/mis-pedidos",
  "description": "Listado de ordenes del dropshipper con tabla, filtros, acciones masivas y detalle por fila.",
  "profiles": ["dropshipper"],
  "owner": "producto@dropi.co",
  "dateAdded": "2026-05-01",
  "thumbnail": "thumbnail.png"
}
```

**6. src/app/pages/orders-provider/meta.json**
```json
{
  "title": "Mis Pedidos Proveedor",
  "module": "pedidos",
  "slug": "mis-pedidos-proveedor",
  "route": "/pedidos/mis-pedidos-proveedor",
  "description": "Pedidos recibidos por el proveedor con tabs Mis Pedidos y Datos Facturacion, filtros y exportacion Excel.",
  "profiles": ["proveedor"],
  "owner": "producto@dropi.co",
  "dateAdded": "2026-05-01",
  "thumbnail": "thumbnail.png"
}
```

**7. src/app/pages/historial-cartera/meta.json**
```json
{
  "title": "Historial de Cartera",
  "module": "financiero",
  "slug": "historial-cartera",
  "route": "/historial-de-cartera",
  "description": "Historial de cartera con filtros de fecha, tipo y codigo. Tabs transacciones y depositos manuales.",
  "profiles": ["dropshipper"],
  "owner": "producto@dropi.co",
  "dateAdded": "2026-05-01",
  "thumbnail": "thumbnail.png"
}
```

**8. src/app/pages/dropicard/meta.json**
```json
{
  "title": "Dropi Card",
  "module": "standalone",
  "slug": "dropicard",
  "route": "/dropi-card/cards",
  "description": "Gestion de tarjetas Dropicard con banner promocional, filtros por tipo y estado, solicitar tarjeta.",
  "profiles": ["dropshipper", "proveedor"],
  "owner": "producto@dropi.co",
  "dateAdded": "2026-05-01",
  "thumbnail": "thumbnail.png"
}
```

**9. src/app/pages/cas/meta.json**
```json
{
  "title": "CAS Bandeja",
  "module": "cas",
  "slug": "cas-bandeja",
  "route": "/cas/bandeja",
  "description": "Bandeja de entrada CAS con panel de conversaciones y chat de soporte.",
  "profiles": ["dropshipper"],
  "owner": "producto@dropi.co",
  "dateAdded": "2026-05-01",
  "thumbnail": "thumbnail.png"
}
```

**10. src/app/pages/academy/meta.json**
```json
{
  "title": "Academy",
  "module": "standalone",
  "slug": "academy",
  "route": "/academy",
  "description": "Centro de formacion con cursos y tutoriales para dropshippers.",
  "profiles": ["dropshipper"],
  "owner": "producto@dropi.co",
  "dateAdded": "2026-05-01",
  "thumbnail": "thumbnail.png"
}
```

CRITICAL per D-05: Do NOT modify any existing .ts, .html, .scss, or route file. Only CREATE new meta.json files.
  </action>
  <verify>
    <automated>find src/app/pages -name "meta.json" | wc -l | tr -d ' ' | grep -q "10" && echo "PASS: 10 meta.json files" || echo "FAIL: expected 10 meta.json files"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 10 meta.json files exist under src/app/pages/
    - Each meta.json has all 9 fields: title, module, slug, route, description, profiles, owner, dateAdded, thumbnail
    - `profiles` is an array in every file (not a string)
    - `owner` ends with `@dropi.co` in every file
    - `dateAdded` is ISO date format in every file
    - `route` matches the actual route in app.routes.ts for each prototype
    - No existing .ts, .html, .scss files were modified (verify with `git diff --name-only` showing only new files)
  </acceptance_criteria>
  <done>All 10 prototypes have valid meta.json files in-place next to their components, with accurate routes, profiles, and descriptions matching navigation-map.json</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta.json -> registry | Untrusted JSON files consumed by build script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | meta.json files | accept | Files are committed to repo, reviewed in PR. No runtime execution of JSON content. Phase 3 CI adds Zod validation. |
| T-02-02 | Information Disclosure | owner field in meta.json | accept | Owner emails are @dropi.co internal, not sensitive PII. Hub is behind auth anyway. |
</threat_model>

<verification>
- `find src/app/pages -name "meta.json" | wc -l` returns 10
- Each meta.json is valid JSON: `for f in src/app/pages/*/meta.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'))" && echo "OK: $f"; done`
- No existing files modified: `git diff --name-only` shows 0 modified files (only untracked new files)
- All 9 required fields present in each file: `for f in src/app/pages/*/meta.json; do node -e "const m=JSON.parse(require('fs').readFileSync('$f','utf8'));const k=['title','module','slug','route','description','profiles','owner','dateAdded','thumbnail'];const missing=k.filter(x=>!m[x]);if(missing.length)throw new Error('Missing: '+missing);console.log('OK: '+m.title)"; done`
</verification>

<success_criteria>
- PrototypeMeta interface exported from src/app/models/prototype-meta.model.ts
- 10 meta.json files created in-place, no migration
- Zero modifications to existing prototype files
- All meta.json files parse as valid JSON with all 9 required fields
</success_criteria>

<output>
After completion, create `.planning/phases/02-hub-home-prototype-structure/02-01-SUMMARY.md`
</output>
