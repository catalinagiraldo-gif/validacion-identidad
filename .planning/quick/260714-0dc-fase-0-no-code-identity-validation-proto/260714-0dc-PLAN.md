---
phase: quick-260714-0dc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/common/models/identity-flow-v2.models.ts
  - src/app/common/services/identity-demo-state-v2.service.ts
  - src/app/common/services/identity-fase0.service.ts
  - src/app/common/components/identity-fase0-panel/identity-fase0-panel.component.ts
  - src/app/common/components/identity-fase0-panel/identity-fase0-panel.component.html
  - src/app/common/components/identity-fase0-panel/identity-fase0-panel.component.scss
  - src/app/common/components/identity-fase0-interceptor/identity-fase0-interceptor.component.ts
  - src/app/common/components/identity-fase0-interceptor/identity-fase0-interceptor.component.html
  - src/app/common/components/identity-fase0-interceptor/identity-fase0-interceptor.component.scss
  - src/app/common/components/identity-fase0-block/identity-fase0-block.component.ts
  - src/app/common/components/identity-fase0-block/identity-fase0-block.component.html
  - src/app/common/components/identity-fase0-block/identity-fase0-block.component.scss
  - src/app/common/components/identity-fase0-result/identity-fase0-result.component.ts
  - src/app/common/components/identity-fase0-result/identity-fase0-result.component.html
  - src/app/common/components/identity-fase0-result/identity-fase0-result.component.scss
  - src/app/common/components/identity-fase0-crm-toast/identity-fase0-crm-toast.component.ts
  - src/app/common/components/identity-fase0-crm-toast/identity-fase0-crm-toast.component.html
  - src/app/common/components/identity-fase0-crm-toast/identity-fase0-crm-toast.component.scss
  - src/app/common/components/identity-soft-banner/identity-soft-banner.component.ts
  - src/app/common/components/identity-gate/identity-gate.component.ts
  - src/app/pages/new/home/home.component.ts
  - src/app/pages/new/financiero/wallet/wallet.component.ts
  - src/app/pages/new/financiero/datos-facturacion/datos-facturacion.component.ts
  - src/app/pages/new/configurar/cuenta/cuenta.component.ts
  - src/app/layout/layout-new/layout-new.component.ts
  - src/app/layout/layout-new/demo-panel/prototype-demo-panel.component.ts
  - src/app/layout/layout-new/demo-panel/prototype-demo-panel.component.html
autonomous: false
requirements: [FASE0-BLUEPRINT]
must_haves:
  truths:
    - "Con faseProyecto()==='fase0', Home muestra un Panel Lateral 'Verifica tu cuenta' (25% ancho, botón X, sin gancho monetario)"
    - "Con fase0, el banner monetario identity-soft-banner y el modal completo identity-sumsub-modal NO aparecen"
    - "Con fase0, clic en Transferir Wallet / Datos Bancarios / Solicitud DropiCard / Datos de Facturación / Información de Cuenta abre el Modal Interceptor simple recurrente (sin X)"
    - "El interceptor muestra transición 'Redirigiendo a Sumsub…' y luego vuelve a la página de origen"
    - "Con fase0 y segmento saldo-negativo/fraude, Retiro o Envío abre el Modal Full-Screen de bloqueo sin salida"
    - "Los 4 modales de Etapa Continua (aprobado autodismiss 4-5s, revisión-financiero, incompleta que reabre interceptor, rechazado) muestran copy textual del blueprint"
    - "El demo-panel simula 4 mensajes CRM como burbuja WhatsApp con copy textual, y permite navegar todos los casos Fase 0"
    - "Con fase1+ el comportamiento heredado (soft-banner monetario + sumsub modal completo + identity-gate) sigue funcionando sin cambios"
  artifacts:
    - path: "src/app/common/services/identity-fase0.service.ts"
      provides: "Orquestador de overlays Fase 0 (interceptor/bloqueo/resultado/CRM) + tryIntercept()"
    - path: "src/app/common/components/identity-fase0-interceptor/identity-fase0-interceptor.component.ts"
      provides: "Modal interceptor Etapa 0.5 + transición a Sumsub"
    - path: "src/app/common/components/identity-fase0-block/identity-fase0-block.component.ts"
      provides: "Modal full-screen de bloqueo Etapa 1"
    - path: "src/app/common/components/identity-fase0-result/identity-fase0-result.component.ts"
      provides: "4 modales de resultado Etapa Continua"
    - path: "src/app/common/components/identity-fase0-panel/identity-fase0-panel.component.ts"
      provides: "Panel lateral Etapa 0 en Home"
  key_links:
    - from: "src/app/pages/new/financiero/wallet/wallet.component.ts"
      to: "IdentityFase0Service.tryIntercept"
      via: "guard al inicio de onTransferir/onRetirar"
      pattern: "tryIntercept"
    - from: "src/app/layout/layout-new/layout-new.component.ts"
      to: "identity-fase0-* overlays"
      via: "render global"
      pattern: "app-identity-fase0"
---

<objective>
Cuando `IdentityDemoStateV2Service.faseProyecto() === 'fase0'`, TODO lo que ve el usuario en las rutas `/new/*` debe coincidir exactamente con el Service Blueprint de Fase 0 (proceso 100% NO-CODE): un Panel Lateral pedagógico sin gancho monetario, un Modal Interceptor simple y recurrente al intentar acciones financieras, un Modal de Bloqueo full-screen para cuentas en riesgo, y 4 modales de resultado de Etapa Continua — todo con copy TEXTUAL al blueprint. Para fase1+ se conserva intacto el comportamiento heredado (banner monetario, modal Sumsub completo, identity-gate).

Purpose: El prototipo hoy muestra el flujo Sumsub completo (Fase 2/3+) y un banner con gancho monetario incluso en Fase 0 — el blueprint documenta esto como incorrecto para Fase 0. Este plan hace que fase0 sea fiel al blueprint sin tocar la arquitectura heredada.
Output: 1 servicio orquestador nuevo, 5 componentes nuevos, extensión de estado v2, y wiring/suppression condicionado a fase0.
</objective>

<execution_context>
@D:/validacion-identidad/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@docs/validacion/Service_Blueprint_Diagrama Fase 0.md

<interfaces>
<!-- Contratos clave ya en el codebase. Usar directamente, sin re-explorar. -->

IdentityDemoStateV2Service (src/app/common/services/identity-demo-state-v2.service.ts):
  readonly faseProyecto: Signal<FaseProyecto>   // 'fase0' | 'fase1' | ... | 'fase5'
  readonly momentoUsuario: Signal<MomentoUsuario> // 'setup' | 'activacion' | 'habito' | 'profesional' | 'legacy'
  readonly status: Signal<IdentitySatelliteStatus> // 'sin_validar' | 'pendiente' | 'en_revision' | 'rechazado' | 'aprobado'
  setStatus(s), setSegmentoUsuario(s), setEmailEnListaNegra(b)
  // patrón de cada campo: signal privado + asReadonly() + setter que persiste en sessionStorage con clave 'dropi.identityV2.<campo>' + loader privado

IdentityModalService (src/app/common/services/identity-modal.service.ts):
  type OrigenModal = 'pedidos' | 'home' | 'wallet' | 'retiro' | 'dropicard' | 'facturacion' | 'cuenta'
  open(origen, startScreen?), close(), isOpen, config   // controla identity-sumsub-modal (flujo HEREDADO — no tocar)

IdentityDemoStateService (viejo, src/app/common/services/identity-demo-state.service.ts):
  status, isApproved(), showSoftTouchpoints(), resultadoModal  // driver de soft-banner / identity-gate / sumsub-modal (HEREDADO)

layout-new.component.ts renderiza globalmente <app-identity-sumsub-modal />; los overlays Fase 0 se agregan igual.
Tokens SCSS en src/styles/_variables.scss: $primary-500 #f49a3d, $gray-*, $white, $font-primary Inter, $size-*, $radius-*, $shadow-medium, $bp-lg/$bp-md/$bp-sm.
DS Registry: ds-registry/components/dropi-modal.json, dropi-button.json, dropi-alert.json.
</interfaces>
</context>

<constraints_recordatorio>
- COPY 100% TEXTUAL al blueprint. Cero parafraseo, cero traducción, cero "mejora".
- TODO condicionado a `stateV2.faseProyecto() === 'fase0'`. fase1+ intacto.
- Angular 17.3 standalone + signals, mismo patrón de los servicios/componentes existentes.
- Estilos SOLO con tokens de _variables.scss. Responsive obligatorio ($bp-lg/$bp-md/$bp-sm) en cada componente nuevo.
- No se instalan paquetes nuevos (todo Angular existente).
</constraints_recordatorio>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Estado v2 extendido + servicio orquestador IdentityFase0Service</name>
  <files>
    src/app/common/models/identity-flow-v2.models.ts,
    src/app/common/services/identity-demo-state-v2.service.ts,
    src/app/common/services/identity-fase0.service.ts
  </files>
  <action>
En identity-flow-v2.models.ts añadir tipos exportados (aditivos, no modificar existentes): `MotivoPendiente = 'revision-financiero' | 'incompleta' | null`; `Fase0ResultKind = 'aprobado' | 'revision-financiero' | 'incompleta' | 'rechazado'`; `Fase0CrmKind = 'recordatorio' | 'aprobado' | 'revision-financiero' | 'incompleta'`.

En identity-demo-state-v2.service.ts añadir DOS campos nuevos siguiendo EXACTAMENTE el patrón existente (signal privado + asReadonly() + setter que persiste en sessionStorage + loader privado): (1) `motivoPendiente` de tipo MotivoPendiente, clave 'dropi.identityV2.motivoPendiente', default null, loader que valida 'revision-financiero'|'incompleta' y cae a null; (2) `saldoNegativoFraude` boolean, clave 'dropi.identityV2.saldoNegativoFraude', default false. Este toggle es DISTINTO de emailEnListaNegra (que es ban cross-country) — representa el segmento "saldo negativo / fraude" del blueprint Etapa 1.

Crear identity-fase0.service.ts (@Injectable providedIn:'root'), inyecta IdentityDemoStateV2Service. Estado por signals privados + asReadonly(): `interceptorOpen` (bool), `interceptorOrigen` (OrigenModal), `redirecting` (bool), `blockOpen` (bool), `activeResult` (Fase0ResultKind|null), `crmMessage` ({kind:Fase0CrmKind,text:string}|null). Métodos:
  - `openInterceptor(origen: OrigenModal)`: setea interceptorOrigen, interceptorOpen=true, redirecting=false.
  - `continueToSumsub()`: redirecting=true; tras ~1400ms via setTimeout: redirecting=false, interceptorOpen=false (esto ES "volver a la página de origen" — el interceptor es un overlay sobre la página actual, cerrarlo devuelve al origen). Guardar el id del timeout y limpiarlo si se reabre.
  - `closeInterceptor()`.
  - `openBlock()`, `closeBlock()` (closeBlock solo lo usa el demo-panel para resetear; en producto es persistente).
  - `showResult(kind: Fase0ResultKind)`: activeResult=kind; si kind==='aprobado' programa dismissResult() a ~4500ms (autodesaparece 4-5s).
  - `dismissResult()`.
  - `showCrmMessage(kind: Fase0CrmKind)`: mapea kind→copy TEXTUAL (ver strings abajo) y setea crmMessage; auto-dismiss a ~6000ms.
  - `dismissCrm()`.
  - `tryIntercept(origen: OrigenModal, esRetiroOEnvio: boolean): boolean`: si faseProyecto()!=='fase0' retorna false (el llamador ejecuta su flujo heredado); si es fase0 y saldoNegativoFraude() && esRetiroOEnvio → openBlock() y retorna true; si es fase0 → openInterceptor(origen) y retorna true.
Copys CRM TEXTUALES para showCrmMessage:
  recordatorio: "Hola 👋 Notamos que aún no verificas tu cuenta en Dropi. Lo necesitamos para confirmar quién eres y mantener la plataforma segura — no toma más de unos minutos."
  aprobado: "Hola 👋 ¡tu cuenta en Dropi ya está verificada! Ya puedes transferir tu wallet, registrar tus datos bancarios y pedir tu DropiCard sin restricciones. Aprovecha y sigue haciendo crecer tu negocio 🚀"
  revision-financiero: "Hola 👋 tu verificación en Dropi sigue en proceso — puede tardar hasta 72 horas hábiles. Te avisaremos apenas esté lista, no necesitas hacer nada más. ¿Dudas? Escríbenos."
  incompleta: "Hola 👋 empezaste tu verificación en Dropi pero no la terminaste. Complétala para seguir operando sin restricciones: [link a Sumsub]."
  </action>
  <verify>
    <automated>grep -q "saldoNegativoFraude" src/app/common/services/identity-demo-state-v2.service.ts && grep -q "motivoPendiente" src/app/common/services/identity-demo-state-v2.service.ts && grep -q "tryIntercept" src/app/common/services/identity-fase0.service.ts && grep -q "Redirig\|redirecting" src/app/common/services/identity-fase0.service.ts</automated>
  </verify>
  <done>Los 3 archivos compilan; el servicio expone tryIntercept, openInterceptor, continueToSumsub, openBlock, showResult, showCrmMessage con los copys CRM textuales; el estado v2 persiste motivoPendiente y saldoNegativoFraude en sessionStorage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Panel Lateral (Etapa 0) + Modal Interceptor (Etapa 0.5)</name>
  <files>
    src/app/common/components/identity-fase0-panel/identity-fase0-panel.component.ts,
    src/app/common/components/identity-fase0-panel/identity-fase0-panel.component.html,
    src/app/common/components/identity-fase0-panel/identity-fase0-panel.component.scss,
    src/app/common/components/identity-fase0-interceptor/identity-fase0-interceptor.component.ts,
    src/app/common/components/identity-fase0-interceptor/identity-fase0-interceptor.component.html,
    src/app/common/components/identity-fase0-interceptor/identity-fase0-interceptor.component.scss,
    src/app/pages/new/home/home.component.ts,
    src/app/layout/layout-new/layout-new.component.ts
  </files>
  <action>
Crear IdentityFase0PanelComponent (standalone, selector app-identity-fase0-panel). Inyecta IdentityDemoStateV2Service. `isVisible` computed = faseProyecto()==='fase0' && momentoUsuario() ∈ {'setup','activacion'} && !dismissed(). Panel lateral fijo (position:fixed, lado derecho), ancho 25% de viewport (usar width:25vw con min-width y max-width razonables), altura completa del área de contenido, `$shadow-medium`, fondo $white. Contenido TEXTUAL: headline "Verifica tu cuenta"; body "En Dropi lo necesitamos para confirmar quién eres y mantener la plataforma segura."; botón primario "Verificar ahora" (estilo dropi-button "New", $primary-500) que llama `fase0.openInterceptor('home')`; botón 'X' en la esquina con `aria-label="Cerrar, verificar más tarde"` que llama `dismiss()`. SIN incentivo monetario, sin mención de retiro/saldos. Responsive: a `$bp-md` el panel pasa a ocupar todo el ancho (100vw) como hoja inferior o full overlay; a `$bp-sm` reduce padding de $size-8 a $size-4.

Crear IdentityFase0InterceptorComponent (standalone, selector app-identity-fase0-interceptor). Inyecta IdentityFase0Service. Overlay modal centrado (position:fixed, backdrop con blur, scroll interno, header/footer fijos según reglas del CLAUDE.md). Visible cuando `fase0.interceptorOpen()`. SIN botón 'X' (obliga a continuar). Copy TEXTUAL: headline "Verifica tu identidad para continuar"; body "Antes de mover fondos necesitamos confirmar quién eres. Te llevamos a completar la verificación."; botón único "Continuar a verificación" que llama `fase0.continueToSumsub()`. Cuando `fase0.redirecting()` es true, reemplazar el cuerpo/botón por una transición breve: spinner + texto "Redirigiendo a Sumsub…" (no inventar UI de Sumsub — es un redirect externo). El texto del modal NO varía entre reapariciones. Estilos con tokens; DS dropi-modal/dropi-button. Responsive obligatorio.

En home.component.ts: importar IdentityFase0PanelComponent, agregarlo a imports, y renderizar `<app-identity-fase0-panel />` dentro de .page-wrapper (arriba). NO quitar el soft-banner existente (su supresión en fase0 se hace en Task 4). NO tocar nada más de Home.

En layout-new.component.ts: importar IdentityFase0InterceptorComponent, agregarlo a imports y renderizar `<app-identity-fase0-interceptor />` junto a `<app-identity-sumsub-modal />`.
  </action>
  <verify>
    <automated>grep -q "Verifica tu cuenta" src/app/common/components/identity-fase0-panel/identity-fase0-panel.component.html && grep -q "Verifica tu identidad para continuar" src/app/common/components/identity-fase0-interceptor/identity-fase0-interceptor.component.html && grep -q "Redirigiendo a Sumsub" src/app/common/components/identity-fase0-interceptor/identity-fase0-interceptor.component.html && grep -q "app-identity-fase0-panel" src/app/pages/new/home/home.component.ts && grep -q "app-identity-fase0-interceptor" src/app/layout/layout-new/layout-new.component.ts</automated>
  </verify>
  <done>Panel lateral e interceptor existen con copy textual y aria-label correcto, renderizados en Home y layout-new respectivamente; el interceptor no tiene botón X y muestra la transición "Redirigiendo a Sumsub…".</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Modal Bloqueo (Etapa 1) + 4 Modales de Resultado (Etapa Continua)</name>
  <files>
    src/app/common/components/identity-fase0-block/identity-fase0-block.component.ts,
    src/app/common/components/identity-fase0-block/identity-fase0-block.component.html,
    src/app/common/components/identity-fase0-block/identity-fase0-block.component.scss,
    src/app/common/components/identity-fase0-result/identity-fase0-result.component.ts,
    src/app/common/components/identity-fase0-result/identity-fase0-result.component.html,
    src/app/common/components/identity-fase0-result/identity-fase0-result.component.scss,
    src/app/layout/layout-new/layout-new.component.ts
  </files>
  <action>
Crear IdentityFase0BlockComponent (standalone, selector app-identity-fase0-block). Inyecta IdentityFase0Service. Modal PANTALLA COMPLETA (position:fixed inset:0, cubre todo, sin backdrop-click de cierre), visible cuando `fase0.blockOpen()`. SIN botón de cierre, persistente. Copy TEXTUAL: headline "Bloqueamos esta operación por seguridad"; body "Estamos revisando tu cuenta para proteger tus fondos. No podrás hacer retiros ni envíos mientras dure la revisión."; botón único "Contactar a soporte". El botón "Contactar a soporte" es un afordance de prototipo (no destino real): al hacer clic muestra un estado breve tipo "Abriendo canal de soporte…" y no cierra el modal (el reset del bloqueo lo hace el demo-panel en Task 5). Responsive obligatorio.

Crear IdentityFase0ResultComponent (standalone, selector app-identity-fase0-result). Inyecta IdentityFase0Service. Overlay modal centrado, visible cuando `fase0.activeResult()` no es null. Renderiza UNO de 4 estados según activeResult() con copy TEXTUAL:
  - 'aprobado': headline "¡Cuenta verificada!"; body "Ya puedes transferir tu wallet, registrar tus datos bancarios y pedir tu DropiCard."; SIN botón (se autodesaparece a los 4-5s — el timer ya vive en el servicio showResult).
  - 'revision-financiero': headline "Tu verificación sigue en proceso"; body "Puede tardar hasta 72 horas hábiles. Te avisaremos apenas esté lista — no necesitas hacer nada más."; botón "Entendido" → `fase0.dismissResult()`.
  - 'incompleta': headline "Tu verificación quedó incompleta"; body "Empezaste el proceso pero no lo terminaste en Sumsub. Complétalo para poder operar sin restricciones."; botón "Continuar verificación" → dismissResult() y luego `fase0.openInterceptor('home')` (reabre el interceptor de Etapa 0.5).
  - 'rechazado': headline "No pudimos verificar tu identidad"; body "Por seguridad, restringimos las operaciones de esta cuenta. Si crees que es un error, contáctanos y lo revisamos."; botón "Contactar a soporte" (mismo afordance de prototipo que en el bloqueo) → dismissResult().
Usar DS dropi-modal/dropi-button, tokens, iconografía sutil por estado (verde éxito $success, azul info, rojo $error) sin salir de los tokens. Responsive obligatorio.

En layout-new.component.ts: importar y renderizar `<app-identity-fase0-block />` y `<app-identity-fase0-result />` junto a los overlays anteriores.
  </action>
  <verify>
    <automated>grep -q "Bloqueamos esta operación por seguridad" src/app/common/components/identity-fase0-block/identity-fase0-block.component.html && grep -q "¡Cuenta verificada!" src/app/common/components/identity-fase0-result/identity-fase0-result.component.html && grep -q "Tu verificación quedó incompleta" src/app/common/components/identity-fase0-result/identity-fase0-result.component.html && grep -q "No pudimos verificar tu identidad" src/app/common/components/identity-fase0-result/identity-fase0-result.component.html && grep -q "app-identity-fase0-block" src/app/layout/layout-new/layout-new.component.ts && grep -q "app-identity-fase0-result" src/app/layout/layout-new/layout-new.component.ts</automated>
  </verify>
  <done>Bloqueo full-screen sin salida y los 4 modales de resultado existen con copy textual, renderizados en layout-new; 'aprobado' sin botón (autodismiss por servicio), 'incompleta' reabre el interceptor.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wiring de triggers (tryIntercept) + supresión de UI heredada en fase0</name>
  <files>
    src/app/common/components/identity-soft-banner/identity-soft-banner.component.ts,
    src/app/common/components/identity-gate/identity-gate.component.ts,
    src/app/pages/new/financiero/wallet/wallet.component.ts,
    src/app/pages/new/financiero/datos-facturacion/datos-facturacion.component.ts,
    src/app/pages/new/configurar/cuenta/cuenta.component.ts
  </files>
  <action>
Objetivo: en fase0 los clics en acciones financieras abren el interceptor/bloqueo Fase 0 en vez del modal Sumsub heredado, y el banner monetario NO aparece. fase1+ queda idéntico.

identity-soft-banner.component.ts: inyectar IdentityDemoStateV2Service; en el computed `isVisible` añadir `&& this.stateV2.faseProyecto() !== 'fase0'`. (En fase0 el banner monetario desaparece; su rol pedagógico lo cubre el Panel Lateral de Home.) No tocar copys ni variantes.

identity-gate.component.ts: inyectar IdentityFase0Service. En `onCta()` añadir al inicio: `if (this.fase0.tryIntercept(this.origenFromContexto, this.contexto === 'retiro')) return;` antes de la lógica heredada (que abre el sumsub modal). Así en fase0 el gate de dropicard/retiro dispara el interceptor (o el bloqueo si contexto retiro + saldo negativo), y en fase1+ mantiene su comportamiento. Mantener el gate VISIBLE en fase0 (es un blocker neutral, no monetario) — no suprimirlo.

wallet.component.ts: inyectar IdentityFase0Service. En `onTransferir()` añadir al inicio `if (this.fase0.tryIntercept('wallet', false)) return;` (transferir = Etapa 0.5, nunca bloqueo). En `onRetirar()` añadir al inicio `if (this.fase0.tryIntercept('retiro', true)) return;` (retiro = puede ser bloqueo Etapa 1 si saldo negativo/fraude). Conservar debajo la lógica heredada existente.

datos-facturacion.component.ts: inyectar IdentityFase0Service. En el método que hoy hace `modalSvc.open('facturacion', ...)` (onCta / equivalente), añadir al inicio `if (this.fase0.tryIntercept('facturacion', false)) return;`.

cuenta.component.ts: inyectar IdentityFase0Service. En el método que hoy hace `modalSvc.open('cuenta', ...)` añadir al inicio `if (this.fase0.tryIntercept('cuenta', false)) return;`.

No modificar ninguna otra lógica de estas páginas ni el identity-sumsub-modal.
  </action>
  <verify>
    <automated>grep -q "faseProyecto() !== 'fase0'" src/app/common/components/identity-soft-banner/identity-soft-banner.component.ts && grep -q "tryIntercept" src/app/common/components/identity-gate/identity-gate.component.ts && grep -q "tryIntercept('wallet'" src/app/pages/new/financiero/wallet/wallet.component.ts && grep -q "tryIntercept('retiro', true)" src/app/pages/new/financiero/wallet/wallet.component.ts && grep -q "tryIntercept('facturacion'" src/app/pages/new/financiero/datos-facturacion/datos-facturacion.component.ts && grep -q "tryIntercept('cuenta'" src/app/pages/new/configurar/cuenta/cuenta.component.ts</automated>
  </verify>
  <done>En fase0: soft-banner oculto, y los triggers (wallet transferir/retirar, datos-facturación, cuenta, identity-gate dropicard/retiro) enrutan por tryIntercept → interceptor o bloqueo. En fase1+ todos los flujos heredados intactos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" tdd="false">
  <name>Task 5: Demo-panel (controles Fase 0) + CRM WhatsApp toast + build y verificación en navegador</name>
  <files>
    src/app/common/components/identity-fase0-crm-toast/identity-fase0-crm-toast.component.ts,
    src/app/common/components/identity-fase0-crm-toast/identity-fase0-crm-toast.component.html,
    src/app/common/components/identity-fase0-crm-toast/identity-fase0-crm-toast.component.scss,
    src/app/layout/layout-new/layout-new.component.ts,
    src/app/layout/layout-new/demo-panel/prototype-demo-panel.component.ts,
    src/app/layout/layout-new/demo-panel/prototype-demo-panel.component.html
  </files>
  <what-built>
Crear IdentityFase0CrmToastComponent (standalone, selector app-identity-fase0-crm-toast). Inyecta IdentityFase0Service. Visible cuando `fase0.crmMessage()` no es null. Estilo BURBUJA DE WHATSAPP (fondo verde WhatsApp — usar un verde dentro del rango de tokens: preferir $success #4caf50 o una variante; header con avatar/ícono de WhatsApp y "Dropi", cuerpo = crmMessage().text, hora simulada), esquina inferior derecha, animación de entrada, auto-dismiss (timer ya en el servicio) + botón cerrar → `fase0.dismissCrm()`. Renderizar `<app-identity-fase0-crm-toast />` en layout-new.

Extender prototype-demo-panel (ts + html) con un bloque de controles VISIBLE SOLO cuando `faseProyecto() === 'fase0'` (computed `showFase0Controls`). Inyectar IdentityFase0Service y usar el IdentityDemoStateV2Service ya inyectado. Controles:
  - Toggle "Saldo negativo / fraude": setea `stateV2.setSaldoNegativoFraude(v)`; al apagarlo, llamar `fase0.closeBlock()` para resetear un bloqueo activo. DISTINTO del toggle de lista negra existente.
  - Selector "Motivo pendiente" (visible cuando status()==='pendiente'): 'revision-financiero' | 'incompleta' → `stateV2.setMotivoPendiente(...)`.
  - Grupo "RESULTADO FASE 0" con 4 botones: Aprobado / Pendiente: revisión financiero / Pendiente: incompleta / Rechazado. Cada uno setea el estado coherente (setStatus + setMotivoPendiente cuando aplique) y llama `fase0.showResult(kind)` con el Fase0ResultKind correspondiente.
  - Grupo "SIMULAR MENSAJE CRM" con 4 botones: Recordatorio Etapa 0 / Aprobado / Revisión financiero / Incompleta → `fase0.showCrmMessage(kind)`.
Estilos con tokens y patrón de chips ya existente en el demo-panel. Responsive.

Luego: `yarn build` debe compilar sin errores. Arrancar dev server (`yarn start`) y verificar en el navegador (viewport 1024px) los flujos de Fase 0.
  </what-built>
  <how-to-verify>
1. `yarn build` termina sin errores de compilación.
2. `yarn start`; abrir la app en `/new/*` con el demo-panel en FASE DE ENTREGA = Fase 0.
3. Home + fase0 + momento Setup/Activación: aparece el Panel Lateral "Verifica tu cuenta" (25% ancho, con X, SIN texto de dinero/retiro). Cerrar con X funciona. "Verificar ahora" abre el interceptor.
4. Wallet + fase0: clic "Transferir entre cuentas" abre el Modal Interceptor ("Verifica tu identidad para continuar", sin X). "Continuar a verificación" muestra "Redirigiendo a Sumsub…" y luego cierra volviendo a Wallet. Reintentar reabre el mismo modal con el mismo texto.
5. Verificar que en fase0 NO aparece el banner monetario (soft-banner) ni el modal Sumsub completo.
6. Demo-panel: activar "Saldo negativo / fraude"; en Wallet clic "Retirar" abre el Modal Full-Screen de bloqueo ("Bloqueamos esta operación por seguridad", sin cierre). Apagar el toggle lo resetea.
7. Demo-panel grupo RESULTADO FASE 0: probar los 4 → Aprobado (autodesaparece ~5s, sin botón), Revisión financiero ("Entendido"), Incompleta ("Continuar verificación" reabre interceptor), Rechazado ("Contactar a soporte").
8. Demo-panel SIMULAR MENSAJE CRM: los 4 muestran burbuja WhatsApp con copy textual del blueprint.
9. Cambiar FASE DE ENTREGA a Fase 1+: confirmar que reaparecen el banner monetario y el modal Sumsub completo (comportamiento heredado intacto).
10. Viewport 1024px: sin overlaps, clipping ni scroll horizontal en los overlays nuevos.
  </how-to-verify>
  <resume-signal>Escribe "approved" o describe los problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Prototipo 100% client-side, sin backend, sin red, sin autenticación real ni entrada de datos sensibles. Los overlays Fase 0 son UI simulada controlada por el demo-panel; no cruzan ninguna frontera de confianza nueva.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fase0-01 | Information Disclosure | copy de modales | accept | Sin PII ni datos reales; solo texto estático del blueprint. |
| T-fase0-SC | Tampering | dependencias | accept | No se instalan paquetes nuevos; solo se usa Angular existente. Sin superficie de supply-chain nueva. |
</threat_model>

<verification>
- `yarn build` compila sin errores (gate principal — valida todos los templates Angular nuevos).
- Grep asserts por tarea confirman copy textual y wiring.
- Checkpoint humano en navegador cubre los flujos fase0 y la no-regresión de fase1+.
</verification>

<success_criteria>
- Con faseProyecto()==='fase0', la pantalla coincide con el Service Blueprint de Fase 0 (panel lateral pedagógico, interceptor recurrente, bloqueo full-screen, 4 resultados de Etapa Continua, CRM simulado) con copy TEXTUAL.
- fase1+ conserva intacto el comportamiento heredado (banner monetario, modal Sumsub completo, identity-gate).
- Todos los casos Fase 0 son navegables desde el demo-panel y verificados en navegador.
</success_criteria>

<output>
Al terminar, crear `.planning/quick/260714-0dc-fase-0-no-code-identity-validation-proto/260714-0dc-SUMMARY.md` con lo construido, archivos tocados y resultado de la verificación en navegador.
</output>
