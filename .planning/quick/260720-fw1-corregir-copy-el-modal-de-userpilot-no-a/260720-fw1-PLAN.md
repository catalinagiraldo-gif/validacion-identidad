---
phase: quick-260720-fw1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/validacion/Service_Blueprint_Diagrama Fase 0.md
  - docs/validacion/Service_Blueprint_Diagrama.html
autonomous: true
requirements: [QUICK-260720-fw1]
must_haves:
  truths:
    - "Ningún texto del blueprint afirma que el botón/modal de UserPilot 'abre' el widget de soporte automáticamente"
    - "El copy deja claro que el modal solo le indica al usuario que puede contactar a soporte, y que el usuario abre el widget voluntariamente por su cuenta"
    - "Esto aplica en los 3 puntos de contacto (Bloqueo, Pendiente-en-revisión, Rechazado), en la caja/ítem 'Widget de Soporte', y en la viñeta de Notas finales — en ambos archivos (.md y .html)"
  artifacts:
    - path: "docs/validacion/Service_Blueprint_Diagrama Fase 0.md"
      provides: "5 correcciones de copy (items 1, 2 de Etapa 1; Pendiente y Rechazado de Etapa Continua; Notas finales)"
    - path: "docs/validacion/Service_Blueprint_Diagrama.html"
      provides: "4 correcciones de copy equivalentes, de forma compacta"
---

<objective>
Corrección de precisión técnica señalada por el usuario: el modal de UserPilot (Bloqueo, Pendiente-en-revisión, Rechazado) NO puede abrir el widget de soporte flotante de forma automática/programática — no existe esa integración. Lo único que puede hacer el botón "Contactar a soporte" es DAR INDICACIONES al usuario (vía copy) de que puede contactar a soporte; es el usuario quien, voluntariamente, abre el widget por su cuenta y navega el árbol de opciones.

El texto actual en ambos archivos del blueprint dice cosas como "el botón abre el widget de soporte flotante existente" y "se abre el widget existente" — esto es incorrecto y debe corregirse a un lenguaje que refleje que es una indicación/orientación textual, no una apertura automática.

Decisión ya confirmada con el usuario (no revisitar): no hay integración técnica entre el modal de UserPilot y el widget de soporte. El modal solo orienta/indica; el usuario decide y abre el widget voluntariamente.

Output: 5 correcciones de copy en el `.md` y 4 correcciones equivalentes en el `.html`, sin cambiar estructura, ids, clases ni el árbol de 8 opciones ya definido.
</objective>

<execution_context>
@D:/validacion-identidad/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Nota: Tarea de documentación pura (Markdown/HTML estático). Las reglas de Angular/DS Registry de CLAUDE.md NO aplican.

El HTML tiene su contenido principal en una sola línea física muy larga (línea 17). Para extraer contexto sin mojibake si necesitas volver a verificar algo, usa PowerShell: `Get-Content -Raw -Encoding UTF8`, `.IndexOf(...)`, `.Substring(...)`, escribe a un archivo temporal con `[System.IO.File]::WriteAllText(path, text, [System.Text.Encoding]::UTF8)`, y lee ese archivo temporal con la herramienta Read. Los `old_string`/`new_string` de este plan YA fueron extraídos así y verificados — úsalos EXACTAMENTE como están, no los retipees.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corregir el copy en Service_Blueprint_Diagrama Fase 0.md (5 ediciones)</name>
  <files>docs/validacion/Service_Blueprint_Diagrama Fase 0.md</files>
  <action>
Hacer 5 ediciones EXACTAS con la herramienta Edit. Usa los bloques de abajo LITERALMENTE.

### Edit 1.1 — Etapa 1, item 1 (Modal Pantalla Completa)

old_string:
```
Botón único: **"Contactar a soporte"** (evita dejar al usuario sin ninguna acción disponible). El botón abre el widget de soporte flotante existente; el copy del modal instruye al usuario a tocar el chip raíz **"Validación de identidad"** del widget y luego elegir la opción **"Mi cuenta está bloqueada"** del árbol (deep-link por texto, no por integración; ver ítems 2-4 a continuación).
```

new_string:
```
Botón único: **"Contactar a soporte"** (evita dejar al usuario sin ninguna acción disponible). El botón NO abre el widget de soporte automáticamente — no existe esa integración técnica —; el copy del modal solo le indica al usuario que puede contactar a soporte, orientándolo a abrir por su cuenta el widget de soporte flotante existente, tocar el chip raíz **"Validación de identidad"** y elegir la opción **"Mi cuenta está bloqueada"** del árbol (indicación textual, no deep-link técnico; ver ítems 2-4 a continuación).
```

### Edit 1.2 — Etapa 1, item 2 (Widget de Soporte)

old_string:
```
2. 🔵 Widget de Soporte — se abre el widget de soporte existente; chip raíz **"Validación de identidad"** en su menú principal.
```

new_string:
```
2. 🔵 Widget de Soporte — el usuario lo abre voluntariamente, siguiendo la indicación del modal (no hay apertura automática ni integración); chip raíz **"Validación de identidad"** en su menú principal.
```

### Edit 1.3 — Etapa Continua, Pendiente ("En revisión de Financiero")

old_string:
```
se agrega junto a "Entendido" con la misma lógica de destino que en Etapa 1 y en Rechazado: abre el widget de soporte flotante existente, y el copy instruye al usuario a tocar el chip raíz **"Validación de identidad"** del widget y luego elegir la opción **"Mi verificación sigue en revisión"** del árbol (deep-link por texto, no por integración; mismo árbol de soporte descrito en Etapa 1 → Front stage → Acciones, ítems 2-4).
```

new_string:
```
se agrega junto a "Entendido" con la misma lógica que en Etapa 1 y en Rechazado: el botón NO abre el widget automáticamente — el copy solo le indica al usuario que puede contactar a soporte, orientándolo a abrir por su cuenta el widget de soporte flotante existente, tocar el chip raíz **"Validación de identidad"** y elegir la opción **"Mi verificación sigue en revisión"** del árbol (indicación textual, no integración; mismo árbol de soporte descrito en Etapa 1 → Front stage → Acciones, ítems 2-4).
```

### Edit 1.4 — Etapa Continua, Rechazado

old_string:
```
Botón: **"Contactar a soporte"** — luego expulsión (ver Etapa 1). El botón abre el mismo widget de soporte flotante existente; el copy instruye al usuario a tocar el chip raíz **"Validación de identidad"** del widget y luego elegir la opción **"Me rechazaron la verificación"** del árbol (deep-link por texto, no por integración; mismo árbol de soporte descrito en Etapa 1 → Front stage → Acciones, ítems 2-4).
```

new_string:
```
Botón: **"Contactar a soporte"** — luego expulsión (ver Etapa 1). El botón NO abre el widget automáticamente; el copy solo le indica al usuario que puede contactar a soporte, orientándolo a abrir por su cuenta el mismo widget de soporte flotante existente, tocar el chip raíz **"Validación de identidad"** y elegir la opción **"Me rechazaron la verificación"** del árbol (indicación textual, no integración; mismo árbol de soporte descrito en Etapa 1 → Front stage → Acciones, ítems 2-4).
```

### Edit 1.5 — Notas finales

old_string:
```
los 3 puntos de contacto del blueprint abren el mismo widget de soporte flotante existente y su copy nombra el chip raíz existente **"Validación de identidad"** como punto de entrada textual, orientando a la opción específica del árbol según el caso
```

new_string:
```
los 3 puntos de contacto del blueprint NO abren el widget de soporte automáticamente — no existe esa integración técnica —; su copy solo le indica al usuario que puede contactar a soporte por su cuenta, y nombra el chip raíz existente **"Validación de identidad"** como punto de entrada textual, orientando a la opción específica del árbol según el caso
```

No tocar ninguna otra parte del archivo (el árbol de 8 sub-opciones, la caja "Árbol de opciones (chips)", "Escalamiento", "Si es Criminal", y el bloque "Incompleta en Sumsub" quedan exactamente igual).
  </action>
  <verify>
    <automated>Grep sobre "docs/validacion/Service_Blueprint_Diagrama Fase 0.md": "abre el widget de soporte flotante existente" debe aparecer 0 veces; "abre el widget" (como afirmación de que el botón lo hace) debe aparecer 0 veces; "abre voluntariamente" o "abrir por su cuenta" debe aparecer al menos 3 veces; "Continuar verificación" debe seguir presente.</automated>
  </verify>
  <done>Las 5 ediciones aplicadas; ningún texto afirma apertura automática del widget; el resto del archivo (árbol de 8 opciones, Escalamiento, Si es Criminal, Incompleta en Sumsub) sin cambios.</done>
</task>

<task type="auto">
  <name>Task 2: Corregir el copy en Service_Blueprint_Diagrama.html (4 ediciones)</name>
  <files>docs/validacion/Service_Blueprint_Diagrama.html</files>
  <action>
Hacer 4 ediciones EXACTAS con la herramienta Edit. Usa los bloques de abajo LITERALMENTE (el archivo es una sola línea física larga; los `old_string` son sustrings únicos ya verificados).

### Edit 2.1 — Modal Pantalla Completa (Bloqueo)

old_string:
```
Botón único: "Contactar a soporte" (abre el widget; toca el chip raíz "Validación de identidad" → opción "Mi cuenta está bloqueada" — ver cajas siguientes).
```

new_string:
```
Botón único: "Contactar a soporte" (no abre el widget automático — indica contactar soporte por su cuenta: chip raíz "Validación de identidad" → opción "Mi cuenta está bloqueada" — ver cajas siguientes).
```

### Edit 2.2 — Caja "Widget de Soporte"

old_string:
```
Widget de Soporte — se abre el widget existente; chip raíz "Validación de identidad" en su menú principal.
```

new_string:
```
Widget de Soporte — el usuario lo abre voluntariamente (sin apertura automática); chip raíz "Validación de identidad" en su menú principal.
```

### Edit 2.3 — Pendiente ("En revisión de Financiero")

old_string:
```
Botón secundario: "Contactar a soporte" (abre el widget; toca el chip raíz "Validación de identidad" → opción "Mi verificación sigue en revisión" — mismo árbol de soporte de Etapa 1).
```

new_string:
```
Botón secundario: "Contactar a soporte" (no abre el widget automático — indica contactar soporte por su cuenta: chip raíz "Validación de identidad" → opción "Mi verificación sigue en revisión" — mismo árbol de Etapa 1).
```

### Edit 2.4 — Rechazado

old_string:
```
Botón: "Contactar a soporte" (abre el widget; toca el chip raíz "Validación de identidad" → opción "Me rechazaron la verificación" — mismo árbol de soporte de Etapa 1) — luego expulsión (ver Etapa 1).
```

new_string:
```
Botón: "Contactar a soporte" (no abre el widget automático — indica contactar soporte por su cuenta: chip raíz "Validación de identidad" → opción "Me rechazaron la verificación" — mismo árbol de Etapa 1) — luego expulsión (ver Etapa 1).
```

No tocar ningún otro texto, id, clase, atributo `data-cross-row-*`, ni la estructura de grid/flex. La caja "Árbol de opciones (chips)" con las 8 sub-opciones y la caja "Escalamiento" quedan exactamente igual (no mencionan apertura de widget).
  </action>
  <verify>
    <automated>Grep -o sobre docs/validacion/Service_Blueprint_Diagrama.html: "abre el widget" como frase completa (sin "no abre el widget" delante) debe aparecer 0 veces; "no abre el widget" debe aparecer 4 veces; "data-cross-row" debe seguir apareciendo 30 veces.</automated>
  </verify>
  <done>Las 4 cajas/menciones corregidas; estructura HTML (ids/clases/data-cross-row) intacta; árbol de 8 opciones y caja Escalamiento sin cambios.</done>
</task>

</tasks>

<verification>
- Ninguno de los 2 archivos afirma que el botón/modal "abre" el widget de soporte de forma automática.
- Ambos archivos coinciden en que es el usuario quien abre el widget voluntariamente, guiado por el copy del modal.
- El árbol de 8 opciones, la caja "Árbol de opciones (chips)" y la caja "Escalamiento" quedan sin cambios (no mencionaban apertura de widget).
- `docs/validacion/Soporte-Validacion-Identidad.md` no se toca.
- El HTML mantiene 30 ocurrencias de `data-cross-row`.
</verification>

<success_criteria>
- Corrección de precisión aplicada de forma consistente en los 3 puntos de contacto + caja "Widget de Soporte" + Notas finales, en ambos archivos.
- Sin regresiones estructurales en el HTML.
</success_criteria>

<output>
Create `.planning/quick/260720-fw1-corregir-copy-el-modal-de-userpilot-no-a/260720-fw1-SUMMARY.md` when done
</output>
