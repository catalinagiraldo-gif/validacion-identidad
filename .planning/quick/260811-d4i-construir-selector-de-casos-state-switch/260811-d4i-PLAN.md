---
phase: quick
plan: 260811-d4i
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/common/components/identity-fase15-state-switcher/identity-fase15-state-switcher.component.ts
  - src/app/common/components/identity-fase15-state-switcher/identity-fase15-state-switcher.component.html
  - src/app/common/components/identity-fase15-state-switcher/identity-fase15-state-switcher.component.scss
  - src/app/layout/layout-new/layout-new.component.ts
  - src/app/pages/new/financiero/retiros-saldo/retiros-saldo.component.ts
autonomous: false
requirements: [QUICK-260811-d4i]

must_haves:
  truths:
    - "Con el demo panel en modo Fases 1-5, aparece una pastilla flotante nueva abajo-izquierda en cualquier pagina /new/"
    - "Al expandir la pastilla se ven 3 secciones: IR A LA PAGINA, ESTADO DE IDENTIDAD, ABRIR MODAL DE VERIFICACION"
    - "Cada boton de IR A LA PAGINA navega a la pagina gateada correspondiente"
    - "Cada chip de ESTADO DE IDENTIDAD cambia el estado que refleja el app-identity-gate de la pagina actual"
    - "Cada boton de ABRIR MODAL DE VERIFICACION abre IdentitySumsubModalComponent en la pantalla correcta"
    - "En modo Prototipo 0 (Fase 0) solo aparece el switcher de Fase 0; el nuevo esta oculto"
    - "El boton Procesar de Retiros de Saldo abre el modal de verificacion cuando la identidad no esta aprobada"
  artifacts:
    - path: "src/app/common/components/identity-fase15-state-switcher/identity-fase15-state-switcher.component.ts"
      provides: "Switcher de casos para Fase 1-5 (gating inverso al de Fase 0)"
      contains: "faseProyecto() !== 'fase0'"
    - path: "src/app/common/components/identity-fase15-state-switcher/identity-fase15-state-switcher.component.html"
      provides: "Template con 3 secciones (paginas / estados / modal)"
    - path: "src/app/common/components/identity-fase15-state-switcher/identity-fase15-state-switcher.component.scss"
      provides: "Estilos con prefijo BEM .fase15-switcher, clonados del switcher de Fase 0"
  key_links:
    - from: "src/app/layout/layout-new/layout-new.component.ts"
      to: "IdentityFase15StateSwitcherComponent"
      via: "imports array + tag en el template"
      pattern: "app-identity-fase15-state-switcher"
    - from: "identity-fase15-state-switcher.component.ts"
      to: "IdentityDemoStateService.setStatus"
      via: "chips de estado"
      pattern: "setStatus\\("
    - from: "identity-fase15-state-switcher.component.ts"
      to: "IdentityModalService.open"
      via: "botones de modal"
      pattern: "modalSvc\\.open\\("
    - from: "src/app/pages/new/financiero/retiros-saldo/retiros-saldo.component.ts"
      to: "IdentityModalService.open('retiro', 'screen0')"
      via: "onProcesar() gate"
      pattern: "modalSvc\\.open\\('retiro'"
---

<objective>
Construir `IdentityFase15StateSwitcherComponent`: el selector de casos de un clic para el track Fase 1-5, equivalente al que ya existe para Fase 0. Hoy, para ver cualquier estado de Fase 1-5 hay que combinar a mano ~6 filas de chips del demo panel y navegar uno mismo a la ruta correcta. Con este switcher, un stakeholder salta a cualquier caso con un clic.

De paso se corrige un bug real que impide demostrar el caso "Retiro": `retiros-saldo.component.ts` -> `onProcesar()` es un stub vacio; el boton "Procesar" no hace nada.

Purpose: hacer navegable y demostrable el track que realmente importa (Fase 1-5, el prototipo que vive en `/new/`).
Output: 3 archivos nuevos de componente, registro en `layout-new.component.ts`, y el fix del stub de retiros.

**Fuera de alcance (explicito, no lo hagas):**
- No construir las piezas visuales que le faltan a Fase 1-5 frente a Fase 0 (modal interceptor, toast+confeti, bloqueo pantalla completa). Queda para un pase posterior.
- No tocar el track Fase 0: `IdentityFase0StateSwitcherComponent` y todos los componentes `identity-fase0-*` se quedan exactamente como estan. No ocultar, no borrar, no refactorizar.
- No agregar `fase0.tryIntercept(...)` a `retiros-saldo.component.ts` (seria cambiar comportamiento de Fase 0 en esa pagina — fuera de alcance).
- No tocar `navigation-map.json`: esto es una herramienta flotante de demo, no una vista de prototipo.
</objective>

<execution_context>
@D:/validacion-identidad/.claude/get-shit-done/workflows/execute-plan.md
@D:/validacion-identidad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

Patron a clonar (leer los 3 antes de escribir el componente nuevo):
@src/app/common/components/identity-fase0-state-switcher/identity-fase0-state-switcher.component.ts
@src/app/common/components/identity-fase0-state-switcher/identity-fase0-state-switcher.component.html
@src/app/common/components/identity-fase0-state-switcher/identity-fase0-state-switcher.component.scss

Archivos a modificar:
@src/app/layout/layout-new/layout-new.component.ts
@src/app/pages/new/financiero/retiros-saldo/retiros-saldo.component.ts

<interfaces>
<!-- Contratos ya extraidos del codebase durante planning. Usalos directamente — no explores. -->

De `src/app/common/services/identity-modal.service.ts`:
  export type OrigenModal = 'pedidos' | 'home' | 'wallet' | 'retiro' | 'dropicard' | 'facturacion' | 'cuenta';
  export type StartScreen = 'screen0' | 'screen2' | 'screen3';
  open(origen: OrigenModal, startScreen: StartScreen = 'screen0'): void
  close(): void
  readonly isOpen / readonly config

De `src/app/common/services/identity-demo-state.service.ts`:
  readonly status         // Signal<IdentitySatelliteStatus>  (linea 55)
  readonly isApproved     // computed(() => status() === 'aprobado')  (linea 62)
  setStatus(status: IdentitySatelliteStatus, pais?: PaisPersona): void  (linea 103)
  IdentitySatelliteStatus = 'sin_validar' | 'pendiente' | 'en_revision' | 'rechazado' | 'aprobado'

De `src/app/common/services/identity-demo-state-v2.service.ts`:
  readonly faseProyecto   // Signal<FaseProyecto>, .asReadonly() (linea 91)
  setFaseProyecto(fase: FaseProyecto): void  (linea 156)

Gating actual del switcher de Fase 0 (`identity-fase0-state-switcher.component.ts:45`):
  readonly visible = computed(() => this.stateV2.faseProyecto() === 'fase0');

Rutas confirmadas en `src/app/app.routes.ts` (hijos del shell `/new`):
  'historial-de-cartera'            -> pages/new/financiero/wallet            (contexto="transferencia")
  'financiero/retiros-de-saldo'     -> pages/new/financiero/retiros-saldo     (contexto="retiro")
  'dropi-card/cards'                -> pages/new/financiero/dropicard         (contexto="dropicard")
  'financiero/datos-facturacion'    -> pages/new/financiero/datos-facturacion (contexto="facturacion")
  'configuraciones/cuenta'          -> pages/new/configurar/cuenta            (contexto="cuenta")

Patron del gate en `wallet.component.ts` (lineas 561-567) — YA CONFIRMADO en planning, no lo re-leas:
  onRetirar() llama primero `this.fase0.tryIntercept('retiro', true)` y retorna si intercepta;
  luego `if (!this.stateSvc.isApproved()) { this.modalSvc.open('retiro', 'screen0'); }`.
  Cuando la identidad SI esta aprobada, `onRetirar()` no hace nada mas (solo un comentario:
  el modal de transferencia/retiro real no esta implementado en este prototipo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear IdentityFase15StateSwitcherComponent y registrarlo en layout-new</name>
  <files>
    src/app/common/components/identity-fase15-state-switcher/identity-fase15-state-switcher.component.ts,
    src/app/common/components/identity-fase15-state-switcher/identity-fase15-state-switcher.component.html,
    src/app/common/components/identity-fase15-state-switcher/identity-fase15-state-switcher.component.scss,
    src/app/layout/layout-new/layout-new.component.ts
  </files>
  <action>
    PASO 0 — VERIFICACION OBLIGATORIA ANTES DE ESCRIBIR LOS BOTONES DE RESULTADO.
    El plan aprobado marca este punto como NO confirmado. Antes de cablear los 3 botones de "Resultado"
    (Aprobado / En revision / Rechazado), lee `src/app/common/components/identity-sumsub-modal/identity-sumsub-modal.component.ts`
    y determina como se decide realmente que resultado muestra `screen3`: si lo lee de `IdentityDemoStateService.status()`
    en el momento de abrir, si lo deriva de una signal interna propia del flujo simulado del formulario, o si lo recibe
    por otra via. Ya tienes la firma de `IdentityModalService.open(origen, startScreen)` en `<interfaces>` arriba
    (confirmada en planning: `StartScreen = 'screen0' | 'screen2' | 'screen3'`, `OrigenModal` incluye `'wallet'`),
    asi que el unico archivo que necesitas abrir es el del modal.
    - Si screen3 SI depende de `stateSvc.status()`: cada boton de resultado debe llamar `setStatus(...)` con el estado
      correspondiente ANTES de `modalSvc.open('wallet', 'screen3')`.
    - Si screen3 NO depende de status: usa el mecanismo real que encontraste (por ejemplo un setter o input del propio
      modal) y documenta el hallazgo en el SUMMARY.
    NO asumas que `stateSvc.status()` maneja screen3. Si el mecanismo real no permite forzar los 3 resultados sin tocar
    el modal, para y reporta en vez de improvisar.

    PASO 1 — Componente. Crea `src/app/common/components/identity-fase15-state-switcher/` con los 3 archivos.
    Clona el shell de `IdentityFase0StateSwitcherComponent`: standalone, `imports: [CommonModule]`, `templateUrl` +
    `styleUrls` separados (no inline), signal `expanded` con metodo `toggle()`, pastilla colapsada que expande a panel
    flotante. Copia `identity-fase0-state-switcher.component.scss` tal cual y renombra el prefijo BEM `.fase0-switcher`
    -> `.fase15-switcher` (mismo `position: fixed`, mismo rincon `left: $space-4; bottom: $space-4`, mismo `z-index: 2000`,
    misma animacion translateY + fade). No inventes estilos nuevos ni hardcodees hex: usa unicamente variables de
    `src/styles/_variables.scss` — el SCSS de Fase 0 ya es el precedente DS-compliant de este patron.

    Gating (inverso al de Fase 0, para que nunca compitan por pantalla):
    `readonly visible = computed(() => this.stateV2.faseProyecto() !== 'fase0');`
    Inyecta `IdentityDemoStateV2Service`, `IdentityDemoStateService`, `IdentityModalService` y `Router`.

    PASO 2 — Tres secciones en el template:
    (a) "IR A LA PAGINA" — 5 botones con `router.navigate` a las 5 rutas listadas en `<interfaces>`, prefijadas con `/new`.
        Labels: Wallet / Transferencia, Retiros de saldo, DropiCard, Facturacion, Cuenta.
    (b) "ESTADO DE IDENTIDAD" — 5 chips (Sin validar, Pendiente, En revision, Rechazado, Aprobado) que llaman
        `identityStateSvc.setStatus(...)` con `'sin_validar' | 'pendiente' | 'en_revision' | 'rechazado' | 'aprobado'`.
        El chip del estado actual se resalta con el mismo modificador `--active` que usa el switcher de Fase 0.
    (c) "ABRIR MODAL DE VERIFICACION" — boton "Intro" -> `modalSvc.open('wallet', 'screen0')`; boton "Formulario" ->
        `modalSvc.open('wallet', 'screen2')`; y 3 botones de Resultado (Aprobado / En revision / Rechazado) cableados
        segun lo que confirmaste en el PASO 0. Usa `'wallet'` como origen generico porque el switcher no esta atado a
        una pagina concreta. `screen1` (checklist) no es punto de entrada valido — no lo expongas.

    PASO 3 — Registro. En `src/app/layout/layout-new/layout-new.component.ts`: agrega el import,
    agrega `IdentityFase15StateSwitcherComponent` al array `imports` justo despues de `IdentityFase0StateSwitcherComponent`,
    y agrega `<app-identity-fase15-state-switcher />` en el template justo despues de `<app-identity-fase0-state-switcher />`
    (linea 56). Ambos quedan montados siempre; se auto-excluyen por el gating inverso. No quites nada existente.

    Responsive: verifica que el panel no exceda el viewport a 1024px. Si el SCSS clonado no lo cubre ya, agrega
    `max-width: 100%` / `max-height` con `overflow-y: auto` en el contenedor del panel usando los breakpoints
    `$bp-lg` / `$bp-md` / `$bp-sm` de `_variables.scss`.
  </action>
  <verify>
    <automated>yarn build 2>&1 | tail -20   # compila sin errores nuevos</automated>
    <automated>grep -c "app-identity-fase15-state-switcher" src/app/layout/layout-new/layout-new.component.ts   # >= 1</automated>
    <automated>grep -c "IdentityFase15StateSwitcherComponent" src/app/layout/layout-new/layout-new.component.ts   # == 2 (import + imports array)</automated>
    <automated>grep -c "faseProyecto() !== 'fase0'" src/app/common/components/identity-fase15-state-switcher/identity-fase15-state-switcher.component.ts   # == 1</automated>
    <automated>grep -v "^\s*//" src/app/common/components/identity-fase15-state-switcher/identity-fase15-state-switcher.component.scss | grep -c "fase0-switcher"   # == 0 (prefijo BEM renombrado)</automated>
    <automated>grep -c "app-identity-fase0-state-switcher" src/app/layout/layout-new/layout-new.component.ts   # >= 1 (Fase 0 intacto)</automated>
  </verify>
  <done>
    Los 3 archivos del componente existen, `yarn build` pasa, el componente esta importado y renderizado en
    `layout-new.component.ts` junto al de Fase 0, el gating es `!== 'fase0'`, el SCSS no conserva el prefijo
    `.fase0-switcher`, y el mecanismo real de `screen3` quedo confirmado y documentado (no asumido).
  </done>
</task>

<task type="auto">
  <name>Task 2: Corregir el stub onProcesar() en retiros-saldo</name>
  <files>src/app/pages/new/financiero/retiros-saldo/retiros-saldo.component.ts</files>
  <action>
    Hoy `onProcesar()` (linea ~70) es un stub con el comentario `// stub` — el boton "Procesar" no hace absolutamente
    nada, asi que el caso "Retiro" del nuevo switcher no tendria nada que mostrar.

    Inyecta `IdentityDemoStateService` y `IdentityModalService` (mismo patron que `wallet.component.ts:262-263`) y
    reemplaza el cuerpo del stub por el gate de identidad: si `!this.stateSvc.isApproved()`, llama
    `this.modalSvc.open('retiro', 'screen0')` y retorna.

    Comportamiento cuando la identidad SI esta aprobada: paridad con `wallet.component.ts` -> `onRetirar()`, que ya
    fue verificado en planning y no hace nada mas despues del gate (el flujo real de retiro no existe en este prototipo).
    Deja un comentario equivalente explicandolo, para que quede claro que no es un olvido.

    NO agregues `fase0.tryIntercept(...)`: eso cambiaria el comportamiento del track Fase 0 en esta pagina y esta
    explicitamente fuera de alcance. Solo se inyectan los 2 servicios mencionados.
  </action>
  <verify>
    <automated>grep -c "// stub" src/app/pages/new/financiero/retiros-saldo/retiros-saldo.component.ts   # == 0</automated>
    <automated>grep -c "modalSvc.open('retiro', 'screen0')" src/app/pages/new/financiero/retiros-saldo/retiros-saldo.component.ts   # == 1</automated>
    <automated>grep -c "isApproved()" src/app/pages/new/financiero/retiros-saldo/retiros-saldo.component.ts   # == 1</automated>
    <automated>grep -c "tryIntercept" src/app/pages/new/financiero/retiros-saldo/retiros-saldo.component.ts   # == 0 (fuera de alcance)</automated>
    <automated>yarn build 2>&1 | tail -20   # compila sin errores nuevos</automated>
  </verify>
  <done>
    `onProcesar()` abre `IdentitySumsubModalComponent` con origen `'retiro'` en `screen0` cuando la identidad no esta
    aprobada, y no hace nada adicional cuando si lo esta (paridad con wallet). El build pasa.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Nuevo selector de casos flotante para Fase 1-5 (`IdentityFase15StateSwitcherComponent`) montado en el shell `/new/`,
    con 3 secciones (ir a pagina / estado de identidad / abrir modal), gateado a `faseProyecto !== 'fase0'`.
    Ademas: el boton "Procesar" de Retiros de Saldo ahora abre el modal de verificacion cuando la identidad no esta aprobada.
  </what-built>
  <how-to-verify>
    1. `yarn start` y abre cualquier pagina bajo `/new/`.
    2. En el demo panel, selecciona modo "Prototipo 2 · Fases 1-5". Confirma que aparece la pastilla nueva
       (esquina inferior izquierda) y que la pastilla de Fase 0 NO aparece.
    3. Expande la pastilla. Prueba los 5 botones de "IR A LA PAGINA": cada uno debe navegar a su pagina
       (Wallet, Retiros de saldo, DropiCard, Facturacion, Cuenta).
    4. Prueba los 5 chips de "ESTADO DE IDENTIDAD": el `<app-identity-gate>` de la pagina actual debe reflejar
       el cambio, y el chip activo debe quedar resaltado.
    5. Prueba los 5 botones de "ABRIR MODAL DE VERIFICACION": Intro abre screen0, Formulario abre screen2, y cada
       boton de Resultado abre screen3 mostrando el resultado correcto (aprobado / en revision / rechazado).
    6. Cambia a modo "Prototipo 0 · Fase 0": confirma que solo aparece el switcher de Fase 0 y que su comportamiento
       no cambio en nada.
    7. En `/new/financiero/retiros-de-saldo` con identidad NO aprobada, clic en "Procesar" -> debe abrir el modal.
       Con identidad aprobada -> ya no debe bloquear (no pasa nada, igual que "Retirar" en Wallet).
    8. Redimensiona el navegador a 1024px de ancho: el panel del switcher no debe cortarse ni generar scroll horizontal.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe los problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguno nuevo | El switcher es una herramienta de demo local; no consume input externo, no hace red, no persiste datos nuevos fuera de las signals ya existentes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-d4i-01 | Elevation of Privilege | `IdentityFase15StateSwitcherComponent` (permite forzar `status = 'aprobado'`) | accept | Prototipo de demo sin backend; `IdentityDemoStateService` ya expone `setStatus` desde el demo panel existente. No introduce superficie nueva. |
| T-d4i-02 | Information Disclosure | Panel flotante visible en `/new/` | accept | El hub es privado (allowlist + Google SSO) y el panel no muestra datos reales; mismo criterio ya aplicado al switcher de Fase 0. |
| T-d4i-SC | Tampering | npm installs | accept | Este plan no instala ni actualiza ningun paquete — solo archivos de codigo dentro de `src/`. No aplica el gate de legitimidad. |
</threat_model>

<verification>
- `yarn build` sin errores nuevos.
- El track Fase 0 permanece intacto: `identity-fase0-state-switcher` sigue importado y renderizado en `layout-new.component.ts`, y ningun archivo `identity-fase0-*` fue modificado.
- `navigation-map.json` sin cambios (no es una vista de prototipo).
- Checkpoint humano aprobado (Task 3).
</verification>

<success_criteria>
- Existe `src/app/common/components/identity-fase15-state-switcher/` con `.ts`, `.html` y `.scss`.
- El componente esta gateado a `faseProyecto() !== 'fase0'` y registrado en `layout-new.component.ts`.
- Las 3 secciones funcionan: navegacion a 5 paginas, 5 chips de estado, 5 botones de modal.
- El mecanismo real de `screen3` fue verificado leyendo `identity-sumsub-modal.component.ts` (no asumido) y quedo documentado en el SUMMARY.
- `onProcesar()` en retiros-saldo abre el modal cuando la identidad no esta aprobada.
- Fase 0 sin cambios de comportamiento.
- El panel no se corta ni genera scroll horizontal a 1024px.
</success_criteria>

<output>
Crear `.planning/quick/260811-d4i-construir-selector-de-casos-state-switch/260811-d4i-SUMMARY.md` al terminar.
Incluir explicitamente el hallazgo del PASO 0 (como se determina el resultado de `screen3`).
</output>
