---
phase: quick-260806-r7n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/validacion/Casos-Externos-Referencia-Fase5.md
autonomous: true
requirements: [QUICK-260806-r7n]

must_haves:
  truths:
    - "Existe docs/validacion/Casos-Externos-Referencia-Fase5.md con la investigación externa de casos verificables"
    - "El documento contiene las 14 URLs reales de la fuente, sin URLs inventadas ni omitidas"
    - "El documento sigue el formato narrativo de docs/validacion/ (sin front-matter, encabezado + secciones ##, tablas y bullets)"
    - "El contenido es una transcripción fiel de la sección 8 del plan aprobado — sin casos, cifras ni conclusiones nuevas"
  artifacts:
    - path: "docs/validacion/Casos-Externos-Referencia-Fase5.md"
      provides: "Referencia de casos externos verificados por gap de Dropi para la transición a Fase 5"
      min_lines: 60
      contains: "sumsub.com/customers/cs-money/"
  key_links:
    - from: "docs/validacion/Casos-Externos-Referencia-Fase5.md"
      to: "docs/validacion/Discovery inicial.md"
      via: "sección de corrección de casos citados sin fuente"
      pattern: "Discovery inicial"
    - from: "docs/validacion/Casos-Externos-Referencia-Fase5.md"
      to: "docs/validacion/Discovery-Riesgos-Transicion-Fase5.md"
      via: "documento hermano de la misma transición a Fase 5"
      pattern: "Discovery-Riesgos-Transicion-Fase5"
---

<objective>
Crear `docs/validacion/Casos-Externos-Referencia-Fase5.md`: transcripción fiel al formato narrativo de `docs/validacion/` de la investigación externa de casos de empresas reales y verificables (con URLs), organizados por gap específico de Dropi para la transición a Fase 5.

Purpose: Producto necesita poder citar ante stakeholders casos reales con fuente verificable que respalden las decisiones de diseño de Fase 5 (hard gate solo en salida de dinero, step-up post-KYC, identidad reutilizable entre países), y necesita saber cuáles de los casos ya citados en `Discovery inicial.md` NO tienen fuente localizable antes de mostrarlo a nadie.

Output: Un archivo markdown nuevo. Cero cambios de código.
</objective>

<execution_context>
@D:/validacion-identidad/.claude/get-shit-done/workflows/execute-plan.md
@D:/validacion-identidad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Fuente única del contenido (ya redactado y aprobado por el usuario):
@C:/Users/USER/.claude/plans/los-riesgos-la-experiencia-cryptic-mccarthy.md — leer **líneas 186 a 262**, que corresponden a la sección `## 8. Investigación externa — casos de empresas reales para referenciar` y sus subsecciones 8.1, 8.2, 8.3, 8.4. **NO** transcribir las líneas 264-271 (`### Deliverable propuesto`), que son meta-instrucciones de proceso, no contenido del documento.

Referencia de estilo y formato (documento hermano de la misma carpeta, creado en el quick task 260806-lip):
@docs/validacion/Discovery-Riesgos-Transicion-Fase5.md

Documento auditado por la sección 8.1 (leer solo si hace falta contexto; **NO modificarlo**):
@docs/validacion/Discovery inicial.md

**Convenciones de formato observadas en el documento hermano** (aplicar idénticas):
- Sin front-matter YAML. El archivo abre directamente con `# Título`.
- `## Contexto y propósito` como primera sección, explicando de dónde sale el documento y para qué sirve.
- Secciones numeradas con `## 1.`, `## 2.`, ... y subsecciones `### 1.1`, `### 1.2`.
- Separadores `---` entre secciones de primer nivel.
- Tablas markdown con encabezado `|---|---|---|`.
- Referencias a archivos del repo en backticks: `` `Historia.md` ``, `` `Discovery inicial.md` ``.
- Cierra con una sección `## Verificación` que dice cómo validar el documento con personas reales.
- Todo en español, registro profesional y directo, sin emojis.

**Entorno:** el Bash del sistema falla al hacer `fork` en este equipo (un `wc -l` devolvió exit 66 durante la planificación). Usar **PowerShell** para verificaciones y para git.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transcribir la sección 8 al formato narrativo de docs/validacion/</name>
  <files>docs/validacion/Casos-Externos-Referencia-Fase5.md</files>
  <action>
Crear el archivo transcribiendo **literalmente** el contenido de las líneas 186-262 del plan fuente, con estas adaptaciones de formato (y ninguna otra):

1. **Título del documento (H1)**: `# Casos externos de referencia — empresas verificables por gap de Dropi (transición a Fase 5)`.

2. **`## Contexto y propósito`**: usar el párrafo introductorio de la línea 188 del plan fuente (el que empieza "Investigación con WebSearch/WebFetch...") como base. Puede expandirse en 1-2 oraciones para dar contexto de por qué existe el documento standalone y su relación con `Discovery-Riesgos-Transicion-Fase5.md` (documento hermano de la misma transición) — sin agregar hallazgos, casos ni cifras que no estén en la fuente.

3. **Renumeración**: como el documento es standalone, las subsecciones 8.1 / 8.2 / 8.3 / 8.4 se convierten en secciones de primer nivel numeradas 1 / 2 / 3 / 4:
   - `## 1. Corrección necesaria en Discovery inicial.md` (contenido de 8.1)
   - `## 2. Casos verificados nuevos, organizados por el gap específico de Dropi` (contenido de 8.2)
   - `## 3. Tabla resumen — fuerza de evidencia para citar ante stakeholders` (contenido de 8.3)
   - `## 4. Conclusión` (contenido de 8.4)
   Separar cada sección de primer nivel con `---`, igual que el documento hermano.

4. **Subtítulos dentro de la sección 2**: los siete agrupadores en negrita del original ("Onboarding progresivo...", "Hard gate específico en el momento del retiro...", "Unificación identidad + datos fiscales...", "LATAM — el caso más relevante culturalmente", "KYT cripto en plataformas que no son exchanges...", "Fraude posterior a un KYC aprobado — step-up authentication", "Bloqueo cruzado entre países / identidad compartida...") se promueven a `### 2.1`, `### 2.2`, ... conservando el texto exacto del agrupador como título de la subsección (quitando los dos puntos finales). El orden y el contenido de los bullets bajo cada agrupador no cambia.

5. **Entidades HTML**: las líneas 195 y 196 de la fuente contienen `&lt;1 min` y similares — escribirlas como `<1 min` (menor-que literal) en el markdown final. Es la única sustitución de caracteres permitida.

6. **Tablas**: transcribir las dos tablas (la de 8.1 con columnas "Caso citado en `Discovery inicial.md` | Veredicto | Acción recomendada", y la de 8.3 con "Caso | Gap de Dropi que aborda | Fuerza de evidencia") fila por fila, sin reordenar ni reagrupar filas, conservando el énfasis en negrita tal como está en la fuente (incluido `**Nula — no citar**` en la última fila de la tabla resumen).

7. **URLs**: las 14 URLs del original se copian **exactamente**, con el mismo texto de enlace markdown. Está prohibido inventar, acortar, "arreglar" o completar cualquier URL. Si una URL parece rota o incompleta, se transcribe tal cual y punto.

8. **Cierre `## Verificación`**: agregar como última sección, adaptando la nota de verificación de la línea 270 de la fuente: todas las URLs fueron obtenidas vía WebFetch real durante la investigación (no inventadas); antes de presentar el documento a stakeholders, hacer un click-through de las URLs para confirmar que siguen vivas, porque los case studies de proveedores se reorganizan con frecuencia. Cerrar indicando que no requiere pruebas automatizadas por ser un documento.

**Prohibiciones estrictas para esta tarea:**
- No agregar casos, empresas, cifras, métricas ni conclusiones que no estén en las líneas 186-262 de la fuente.
- No inventar ni modificar URLs.
- No reinterpretar, suavizar ni endurecer ningún veredicto (especialmente el "NO VERIFICADO" de Merama/Kavak/Nuvemshop/VTEX y el "Baja — cifra de marketing propio" de Detelio).
- No transcribir la subsección `### Deliverable propuesto (parte 2, para tu aprobación)` (líneas 264-271).
- No modificar `docs/validacion/Discovery inicial.md` ni ningún otro archivo. La corrección de la sección 1 queda **documentada**, no aplicada — la decisión de aplicarla es del usuario.
- No tocar ningún archivo de código, SCSS, componente Angular ni `navigation-map.json`. Las reglas de DS Registry y wireframes de `CLAUDE.md` no aplican a esta tarea.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='docs/validacion/Casos-Externos-Referencia-Fase5.md'; if (-not (Test-Path $f)) { throw 'FAIL: file missing' }; $c = Get-Content $f -Raw -Encoding UTF8; $u = ([regex]::Matches($c,'https://')).Count; if ($u -lt 14) { throw \"FAIL: only $u URLs\" }; if ($c -notmatch '(?m)\A# ') { throw 'FAIL: no H1 on line 1 (front-matter?)' }; foreach ($p in @('sumsub\.com/customers/cs-money/','jumio\.com/about/press-releases/rappi-onboarding-latin-america/','(?m)^## Contexto','(?m)^## Verificaci','(?m)^## 1\.','(?m)^## 2\.','(?m)^## 3\.','(?m)^## 4\.')) { if ($c -notmatch $p) { throw \"FAIL: missing $p\" } }; if ($c -match 'Deliverable propuesto') { throw 'FAIL: Deliverable propuesto section was transcribed' }; 'OK'"</automated>
  </verify>
  <done>
El archivo existe, no tiene front-matter (línea 1 es `# `), contiene al menos 14 URLs `https://` (las mismas de la fuente), incluye `## Contexto y propósito`, las secciones `## 1.` a `## 4.` y `## Verificación`, y NO contiene la subsección "Deliverable propuesto".
  </done>
</task>

<task type="auto">
  <name>Task 2: Auditar fidelidad de la transcripción y commitear</name>
  <files>docs/validacion/Casos-Externos-Referencia-Fase5.md</files>
  <action>
Auditoría de fidelidad antes de cerrar. Comparar el archivo creado contra las líneas 186-262 del plan fuente y confirmar, punto por punto:

1. **Cobertura de casos** — están presentes todos los casos/entidades nombrados en la fuente: Consensys+Trulioo, iFAST Global Bank, Wirex, Detelio, Merama/Kavak/Nuvemshop/VTEX (como no verificados), Stripe Connect, eBay, Airbnb (onboarding), Uber (contraste), CS.Money+Sumsub, Binance/Coinbase, Twitch, Rappi+Jumio, Mercado Pago/Nubank/Wise LATAM/dLocal/VTEX/Bold/Addi (como no citables), Triple-A+Elliptic, Chainalysis KYT+Authsignal, Revolut, Nubank Defense Platform, Sumsub Reusable KYC (PayDo/Mercuryo), Industry Sharing Safety Program (Uber+Lyft vía HireRight), Airbnb (baneo por asociación).

2. **Integridad de cifras** — verificar que las métricas se transcribieron sin alteración: 73.5% (iFAST), 83% y 1 segundo (Wirex), US$600 (eBay), 62.64% → 92.6% y ~2 horas → 8 segundos y +32% (CS.Money), 750.000 cuentas (Rappi), 30% (Twitch), US$13.5 millones / 92% / 1 de cada 5.000 (Revolut), +10% y -23% (PayDo), +20.000 negocios (Triple-A), 30-60 min → minutos (Consensys), 5x más rápido / -50% (Detelio).

3. **Veredictos intactos** — la tabla de la sección 1 conserva los cinco veredictos exactos (Verificado / Verificado / Verificado con matiz / Parcialmente verificado / NO VERIFICADO), y la tabla de la sección 3 conserva las 14 filas con su nivel de evidencia (Alta / Media / Baja / Nula).

4. **Sin contenido inventado** — ningún caso, URL, métrica o afirmación que no exista en la fuente.

5. **`docs/validacion/Discovery inicial.md` sin modificar** — confirmar con `git status` que el único archivo nuevo bajo `docs/validacion/` que se va a commitear es `Casos-Externos-Referencia-Fase5.md`. (Nota: `Discovery inicial.md` ya aparecía como untracked antes de este task; eso es preexistente y no se toca.)

Si algún punto falla, corregir el archivo antes de commitear.

**Commit** — usar **PowerShell** (el Bash del sistema falla al hacer fork en este entorno):

```
git add "docs/validacion/Casos-Externos-Referencia-Fase5.md"
git commit -m "docs(quick-260806-r7n): add Casos-Externos-Referencia-Fase5.md"
```

Commitear **solo** ese archivo — no usar `git add .` ni `git add -A`, para no arrastrar `docs/validacion/Discovery inicial.md` (untracked preexistente, fuera del alcance de este task).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$msg = (git log -1 --pretty=format:%s); if ($msg -notmatch '260806-r7n') { throw \"FAIL: last commit subject is '$msg'\" }; $files = @(git diff --name-only HEAD~1 HEAD); if ($files.Count -ne 1) { throw \"FAIL: commit touched $($files.Count) files: $($files -join ', ')\" }; if ($files[0] -notmatch 'Casos-Externos-Referencia-Fase5\.md') { throw \"FAIL: wrong file $($files[0])\" }; 'OK'"</automated>
  </verify>
  <done>
Auditoría de los 5 puntos completa sin discrepancias. Existe un commit con mensaje `docs(quick-260806-r7n): ...` que toca exactamente un archivo: `docs/validacion/Casos-Externos-Referencia-Fase5.md`. `docs/validacion/Discovery inicial.md` sigue untracked/sin modificar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contenido externo → documento interno | URLs de terceros citadas en un documento que Producto mostrará a stakeholders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-r7n-01 | Information Disclosure | Casos-Externos-Referencia-Fase5.md | accept | Documento interno de producto, sin PII, sin credenciales, sin datos de usuarios. Solo cifras públicas de terceros ya publicadas |
| T-r7n-02 | Tampering | URLs citadas | mitigate | Prohibición explícita en Task 1 de inventar/alterar URLs; auditoría de fidelidad en Task 2; sección `## Verificación` obliga a click-through antes de presentar a stakeholders |
| T-r7n-03 | Repudiation | Veredictos de verificación | mitigate | Cada caso lleva su nivel de evidencia explícito (Alta/Media/Baja/Nula) en la tabla resumen, de modo que nadie cite un caso no verificado como si lo estuviera |
| T-r7n-SC | Tampering | npm/pip/cargo installs | n/a | Este plan no instala paquetes — es un archivo markdown |
</threat_model>

<verification>
- `docs/validacion/Casos-Externos-Referencia-Fase5.md` existe, sin front-matter, con `# Título` en la línea 1
- Contiene ≥14 URLs `https://`, todas idénticas a las de la fuente
- Contiene las secciones: `## Contexto y propósito`, `## 1.`, `## 2.`, `## 3.`, `## 4.`, `## Verificación`
- NO contiene la subsección "Deliverable propuesto"
- `docs/validacion/Discovery inicial.md` sin modificar
- Ningún archivo de código tocado (`git diff --name-only HEAD~1 HEAD` devuelve exactamente 1 archivo, bajo `docs/`)
- Commit `docs(quick-260806-r7n): ...` creado
</verification>

<success_criteria>
Producto puede abrir `docs/validacion/Casos-Externos-Referencia-Fase5.md`, encontrar para cada gap de Dropi (hard gate en retiro, onboarding progresivo, unificación identidad+fiscal, LATAM, KYT cripto, step-up post-fraude, bloqueo cruzado entre países) al menos un caso de empresa real con URL verificable y su nivel de fuerza de evidencia, y saber exactamente cuáles de los casos ya citados en `Discovery inicial.md` no deben mostrarse a stakeholders sin corregirlos primero — todo sin haber agregado una sola afirmación que no estuviera ya aprobada por el usuario.
</success_criteria>

<output>
Create `.planning/quick/260806-r7n-crear-docs-validacion-casos-externos-ref/260806-r7n-SUMMARY.md` when done
</output>
