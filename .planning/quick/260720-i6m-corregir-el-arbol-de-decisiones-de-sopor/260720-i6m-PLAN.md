---
phase: quick-260720-i6m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/validacion/Soporte-Validacion-Identidad.md
  - docs/validacion/Service_Blueprint_Diagrama Fase 0.md
  - docs/validacion/Service_Blueprint_Diagrama.html
autonomous: true
requirements: [QUICK-260720-i6m]
must_haves:
  truths:
    - "Los chips de cierre 'Es todo por hoy' y 'Necesito la ayuda de un asesor' aparecen documentados como disponibles SOLO al final de la respuesta automática de cada una de las 8 opciones — nunca antes, nunca desde el menú inicial"
    - "Queda explícito que ninguna de las 8 opciones se ramifica en una sub-decisión adicional — todas son respuestas terminales (texto, con enlace/redirección real solo en la opción 4)"
    - "El chip 'Es todo por hoy' se documenta por primera vez (antes solo se mencionaba 'Necesito la ayuda de un asesor')"
    - "Los 3 archivos (Soporte-Validacion-Identidad.md, blueprint .md, blueprint .html) son consistentes entre sí en este punto"
  artifacts:
    - path: "docs/validacion/Soporte-Validacion-Identidad.md"
      provides: "Intro y las 8 filas de la tabla actualizadas con el par de chips de cierre y la aclaración de 'solo al final'"
    - path: "docs/validacion/Service_Blueprint_Diagrama Fase 0.md"
      provides: "Ítems 3 y 4 de Etapa 1 actualizados (Árbol de opciones + renombrado a 'Chips de cierre')"
    - path: "docs/validacion/Service_Blueprint_Diagrama.html"
      provides: "Mismas 2 cajas actualizadas de forma compacta"
---

<objective>
El usuario, viendo una captura real del widget de soporte (Gali, aunque no se nombra en el documento), señaló que los chips "Es todo por hoy" y "Necesito la ayuda de un asesor para mi validación de identidad" aparecen JUNTOS y solo AL FINAL de la respuesta automática de una opción específica — no están disponibles desde el inicio del menú ni antes de recibir esa respuesta. También pidió que el árbol deje claro qué lleva cada decisión: si hay una sub-decisión adicional, o si (como es el caso aquí) no hay más opciones y solo se muestra texto de ayuda (con enlace en un caso, sin enlace en el resto).

El documento `Soporte-Validacion-Identidad.md` ya tenía el patrón correcto por fila ("El agente responde primero...; si el usuario indica que no quedó resuelto, aparece un chip..."), pero:
1. Nunca mencionaba el chip "Es todo por hoy" (solo la escalación).
2. El blueprint (`.md` y `.html`) había simplificado esto a dos cajas secuenciales ("Árbol de opciones" → "Escalamiento") que visualmente sugieren un paso global después del menú, perdiendo el matiz de "por cada opción, al final de su propia respuesta".

Este plan corrige los 3 archivos para reflejar: (a) el par de chips de cierre completo, (b) que aparece solo al final de cada respuesta individual, y (c) que ninguna opción tiene sub-decisiones — son todas terminales.

**Task 1 ya fue ejecutada directamente por el orquestador antes de escribir este plan** (reescritura completa de `Soporte-Validacion-Identidad.md`, archivo pequeño de 27 líneas, redactado como reescritura de archivo completo en vez de edits incrementales). Se documenta aquí igual para mantener el registro completo del quick task. Las Tasks 2 y 3 (blueprint .md y .html) se ejecutan como parte de este mismo plan.

Output: los 3 archivos consistentes: el chip "Es todo por hoy" documentado junto a "Necesito la ayuda de un asesor", ambos como par de cierre al final de cada opción, y la aclaración de que ninguna opción se ramifica.
</objective>

<execution_context>
@D:/validacion-identidad/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Nota: Tarea de documentación pura (Markdown/HTML estático). Las reglas de Angular/DS Registry de CLAUDE.md NO aplican.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir Soporte-Validacion-Identidad.md (COMPLETADA — ver contenido final del archivo)</name>
  <files>docs/validacion/Soporte-Validacion-Identidad.md</files>
  <action>
Reescritura completa del archivo (27 líneas). Cambios aplicados:
- Párrafo 3 del encabezado (nuevo): aclara que ninguna de las 8 opciones se ramifica en sub-decisión, que todas son respuestas terminales (texto, o texto+enlace solo en la opción 4), y que el par de chips de cierre "Es todo por hoy" / "Necesito la ayuda de un asesor para mi validación de identidad" aparece SOLO al final de la respuesta — nunca antes, nunca desde el menú.
- Las 8 filas de la tabla: cada "Acción / Respuesta" ahora termina mencionando el par completo de chips de cierre (antes solo mencionaba "Necesito la ayuda de un asesor"), manteniendo el contenido de negocio de cada fila intacto.
- Nota de diferencia con Argentina: agregada aclaración "(con enlace solo en la opción 4)".
- "## Coordinación": primera viñeta actualizada para mencionar el par de chips de cierre.
- Esta task ya está aplicada en el archivo — no requiere acción adicional, solo verificación.
  </action>
  <verify>
    <automated>Grep sobre docs/validacion/Soporte-Validacion-Identidad.md: "Es todo por hoy" debe aparecer al menos 9 veces (intro + 8 filas); "se ramifica" o "sub-decisión" debe estar presente; las 8 filas `| N |` (N=1..8) siguen presentes; "## Coordinación" presente.</automated>
  </verify>
  <done>El archivo documenta el par completo de chips de cierre, aclara que aparecen solo al final de cada respuesta, y que ninguna opción se ramifica.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar ítems 3 y 4 de Etapa 1 en Service_Blueprint_Diagrama Fase 0.md</name>
  <files>docs/validacion/Service_Blueprint_Diagrama Fase 0.md</files>
  <action>
Dos ediciones con Edit tool.

### Edit 2.1 — Ítem 3 (Árbol de opciones), línea introductoria

old_string:
```
3. 🟣 Árbol de opciones (chips) — dentro del chip raíz "Validación de identidad". El agente responde primero automáticamente según la opción elegida; si el usuario indica que no quedó resuelto, ver ítem 4 (Escalamiento):
```

new_string:
```
3. 🟣 Árbol de opciones (chips) — dentro del chip raíz "Validación de identidad". El agente responde primero automáticamente según la opción elegida. Ninguna de las 8 opciones se ramifica en una sub-decisión adicional — todas son respuestas terminales (texto, o texto con enlace/redirección solo en la opción 4). Solo al final de esa respuesta — nunca antes, ni desde el menú — aparecen los chips de cierre del ítem 4:
```

### Edit 2.2 — Ítem 4 (renombrar de "Escalamiento" a "Chips de cierre")

old_string:
```
4. ⚪ Escalamiento — si el usuario indica que no quedó resuelto, aparece un chip de seguimiento tipo **"Necesito la ayuda de un asesor"** que escala a un asesor humano.
```

new_string:
```
4. ⚪ Chips de cierre (al final de cada opción) — después de la respuesta automática de cada una de las 8 opciones anteriores — no antes, no desde el menú — el agente ofrece juntos dos chips: **"Es todo por hoy"** (cierra la conversación sin escalar) y **"Necesito la ayuda de un asesor"** (escala a un asesor humano si el usuario lo elige).
```

No tocar el resto del archivo (las 8 sub-opciones dentro del ítem 3, ítem 5 "Si es Criminal", Pendiente, Rechazado, Notas finales quedan igual).
  </action>
  <verify>
    <automated>Grep sobre "docs/validacion/Service_Blueprint_Diagrama Fase 0.md": "Chips de cierre" debe aparecer al menos 1 vez; "Es todo por hoy" debe aparecer 1 vez; "se ramifica" presente; "🟣 Si es Criminal" sigue presente sin cambios.</automated>
  </verify>
  <done>Ítems 3 y 4 actualizados con el par de chips de cierre y la aclaración de que ninguna opción se ramifica; resto del archivo sin cambios.</done>
</task>

<task type="auto">
  <name>Task 3: Actualizar las mismas 2 cajas en Service_Blueprint_Diagrama.html</name>
  <files>docs/validacion/Service_Blueprint_Diagrama.html</files>
  <action>
Dos ediciones con Edit tool (archivo de una sola línea física larga; old_strings ya verificados como substrings únicos).

### Edit 3.1 — Caja "Árbol de opciones (chips)", frase introductoria

old_string:
```
🌳 Árbol de opciones (chips) — dentro del chip raíz "Validación de identidad". El agente responde primero automático; si no se resuelve, ver caja "Escalamiento".
```

new_string:
```
🌳 Árbol de opciones (chips) — dentro del chip raíz "Validación de identidad". El agente responde primero automático (ninguna opción tiene sub-decisión — respuesta terminal, con enlace solo en la opción 4); al final de esa respuesta ver caja "Chips de cierre".
```

### Edit 3.2 — Caja "Escalamiento" completa (wrapper + contenido, incluye ensanchar de 160px a 190px)

old_string:
```
<div class="relative w-[160px] flex-none flex justify-center items-center"><div class="relative w-full border-2 rounded-md p-2 text-[10px] leading-tight font-medium text-center shadow-sm z-20 flex flex-col justify-center items-center min-h-[50px] transition-transform hover:scale-[1.02] bg-[#E5E7EB] border-[#9CA3AF] text-[#374151]">Escalamiento — si no quedó resuelto, chip "Necesito la ayuda de un asesor" escala a un asesor humano.</div></div>
```

new_string:
```
<div class="relative w-[190px] flex-none flex justify-center items-center"><div class="relative w-full border-2 rounded-md p-2 text-[10px] leading-tight font-medium text-center shadow-sm z-20 flex flex-col justify-center items-center min-h-[50px] transition-transform hover:scale-[1.02] bg-[#E5E7EB] border-[#9CA3AF] text-[#374151]">Chips de cierre (al final de cada opción, no antes): "Es todo por hoy" (cierra) y "Necesito la ayuda de un asesor" (escala a asesor humano).</div></div>
```

No tocar ningún otro texto, id, clase, atributo `data-cross-row-*`, ni la estructura de grid/flex del resto del archivo. La caja "Widget de Soporte" y el bloque "Si es Criminal" quedan iguales; Pendiente y Rechazado no se tocan.
  </action>
  <verify>
    <automated>Grep -o sobre docs/validacion/Service_Blueprint_Diagrama.html: "Chips de cierre" debe aparecer al menos 1 vez; "Es todo por hoy" debe aparecer 1 vez; "data-cross-row" debe seguir apareciendo 30 veces; "Widget de Soporte" sigue presente.</automated>
  </verify>
  <done>Las 2 cajas actualizadas; estructura HTML intacta (30 data-cross-row); Pendiente/Rechazado sin cambios.</done>
</task>

</tasks>

<verification>
- Los 3 archivos documentan de forma consistente que el par "Es todo por hoy" / "Necesito la ayuda de un asesor" aparece solo al final de cada respuesta, nunca desde el inicio.
- Los 3 archivos aclaran que ninguna de las 8 opciones se ramifica en una sub-decisión — son respuestas terminales, con enlace real solo en la opción 4 (redirección a Sumsub).
- `Pendiente-en-revisión`, `Rechazado`, y el resto de la estructura del blueprint quedan sin cambios.
- El HTML mantiene 30 ocurrencias de `data-cross-row`.
</verification>

<success_criteria>
- El chip "Es todo por hoy" queda documentado por primera vez, junto a "Necesito la ayuda de un asesor", en los 3 archivos.
- Queda explícito en los 3 archivos que este par aparece solo al final de cada opción, no desde el menú.
- Sin regresiones estructurales en el HTML ni contenido de negocio perdido en las 8 filas/opciones.
</success_criteria>

<output>
Create `.planning/quick/260720-i6m-corregir-el-arbol-de-decisiones-de-sopor/260720-i6m-SUMMARY.md` when done
</output>
