---
phase: quick-260811-bf9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/common/components/identity-soft-banner/identity-soft-banner.component.ts
  - src/app/common/components/identity-soft-banner/identity-soft-banner.component.scss
  - src/app/common/components/identity-fase0-panel/identity-fase0-panel.component.html
  - src/app/common/services/identity-fase0.service.ts
  - src/app/layout/layout-new/demo-panel/prototype-demo-panel.component.html
  - docs/validacion/Service_Blueprint_Diagrama_Fase_5.md
  - docs/validacion/Service_Blueprint_Diagrama_Fase_5.html
autonomous: true
requirements: [A2, A3, A4, B1, B2]

must_haves:
  truths:
    - "En Fase 1-5 con Tipo de usuario = Nuevo, Home no muestra ningún aviso de identidad"
    - "En Fase 1-5 con Tipo de usuario = Activo e identidad incompleta, Home muestra el aviso de ganancias como panel flotante en la esquina inferior derecha, cerrable con X"
    - "El toggle Nuevo/Activo del demo panel está disponible en cualquier fase de proyecto, no solo en Fase 0"
    - "El botón del panel Fase 0 dice 'Completar ahora' y el mensaje CRM simulado cita ese mismo texto"
    - "El blueprint Fase 5 (.md y .html) trata 'activo = 20 órdenes' como valor de trabajo confirmado, no como punto abierto 🚧"
    - "La grilla de ETAPA 2 del .html muestra la bifurcación Nuevo/Activo de forma visualmente explícita, no solo como texto en tabla"
  artifacts:
    - path: "src/app/common/components/identity-soft-banner/identity-soft-banner.component.ts"
      provides: "gating del banner de Home por fase0TipoUsuario === 'activo'"
      contains: "fase0TipoUsuario"
    - path: "src/app/common/components/identity-soft-banner/identity-soft-banner.component.scss"
      provides: "modificador --home con posicionamiento fijo esquina inferior derecha + slide-up + breakpoints"
      contains: "&--home"
    - path: "src/app/layout/layout-new/demo-panel/prototype-demo-panel.component.html"
      provides: "toggle TIPO DE USUARIO fuera del bloque @if (showFase0Controls())"
    - path: "docs/validacion/Service_Blueprint_Diagrama_Fase_5.md"
      provides: "umbral de activo resuelto como valor de trabajo"
    - path: "docs/validacion/Service_Blueprint_Diagrama_Fase_5.html"
      provides: "umbral resuelto + fork visual Nuevo/Activo en la grilla ETAPA 2"
  key_links:
    - from: "src/app/common/components/identity-soft-banner/identity-soft-banner.component.ts"
      to: "IdentityDemoStateV2Service.fase0TipoUsuario"
      via: "computed isVisible"
      pattern: "stateV2\\.fase0TipoUsuario\\(\\)\\s*===\\s*'activo'"
    - from: "src/app/common/components/identity-soft-banner/identity-soft-banner.component.html"
      to: ".soft-banner--home"
      via: "[class]='soft-banner--' + variant (Angular merge de class estática + binding)"
      pattern: "soft-banner--"
    - from: "src/app/layout/layout-new/demo-panel/prototype-demo-panel.component.html"
      to: "setFase0TipoUsuario"
      via: "chips del demo panel, ahora fuera del @if de Fase 0"
      pattern: "setFase0TipoUsuario"
---

<objective>
Restaurar la diferenciación Nuevo/Activo en el aviso de verificación de identidad en Home del prototipo `/new/` (hoy sólo existe en el track de demo Fase 0, y se pierde al pasar a Fase 1-5), y reflejar esa misma regla como valor de trabajo confirmado y visualmente explícito en el blueprint Fase 5.

Purpose: el usuario reportó que el aviso "se perdió" y ya no diferencia nuevo/activo al cambiar de la demo Fase 0 al prototipo Fase 1-5. La causa raíz es que `IdentityFase0PanelComponent` está encerrado en `faseProyecto() === 'fase0'`, y en Fase 1-5 Home muestra en su lugar `IdentitySoftBannerComponent(variant="home")` — un banner inline en el top de la página, gatillado por "primera orden entregada", no por "activo".

Output: banner de Home regateado por `fase0TipoUsuario === 'activo'` y reformateado como panel flotante esquina inferior derecha; copy "Completar ahora" unificado; toggle Nuevo/Activo disponible en todas las fases; blueprint `.md`/`.html` con el umbral de 20 órdenes resuelto y el fork Nuevo/Activo visible en la grilla.
</objective>

<execution_context>
@D:/validacion-identidad/.claude/get-shit-done/workflows/execute-plan.md
@D:/validacion-identidad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Decisiones ya cerradas con el usuario en la sesión de planeación previa — NO reabrir:
- **Alcance del fix de código: solo Home (C2).** No se toca C3 (modal hard gate) ni C6 (resultados), aunque el mismo patrón de fragmentación Fase0/Fase1-5 se repite ahí. Queda como hallazgo relacionado, no como trabajo de este pase.
- **El contenido del banner de ganancias en Home se mantiene** (título, subtítulo y CTA actuales). Lo que cambia es el **formato**: pasa a ser el slide-up de esquina inferior derecha, reutilizando el lenguaje visual ya existente en `IdentityFase0PanelComponent`. No inventar una tercera versión visual.
- **Umbral de "activo" = 20 órdenes**, valor de trabajo confirmado (`REUCONTI.md` línea 121). Se simula con el toggle binario `fase0TipoUsuario: 'nuevo' | 'activo'` ya existente; **no** se conecta a `mocks/users.json.totalOrders` en este pase.
- **Copy del botón: "Completar ahora"** (vigente en `Service_Blueprint_Diagrama_Fase_5.md` C2 y en `UX-Writing-Validacion-TechNative.md` §1) reemplaza "Verificar ahora".
- **No se modifica** `navigation-map.json` (Home ya existe, no cambia ruta ni tipo) ni el DS Registry (el patrón de esquina inferior derecha ya está establecido vía `identity-fase0-panel` y `identity-fase0-crm-toast`; se reutiliza, no se inventa).
- **No tocar** `src/app/pages/old/identidad-hub/identidad-hub.component.html` — usa el mismo texto "Verificar ahora" pero es prototipo legado fuera de `/new/`, fuera de alcance.

<interfaces>
<!-- Contratos ya verificados en el codebase. El ejecutor NO necesita explorar. -->

`src/app/common/services/identity-demo-state-v2.service.ts` (sin cambios en este plan):
- línea 104: `readonly fase0TipoUsuario = this._fase0TipoUsuario.asReadonly();` — tipo `Fase0TipoUsuario = 'nuevo' | 'activo'`, vive en el nivel raíz del servicio V2 (no anidado bajo lógica exclusiva de Fase 0), directamente reutilizable.
- línea 265: setter `setFase0TipoUsuario(value)`.
- `faseProyecto()` — signal de fase de proyecto, `'fase0'` vs Fase 1-5.

`identity-soft-banner.component.ts` estado actual (líneas 27-31):
```
readonly isVisible = computed(() =>
  !this.dismissed() &&
  this.stateV2.faseProyecto() !== 'fase0' &&
  this.stateSvc.showSoftTouchpoints()
);
```
`stateV2` ya está inyectado (línea 20). `BannerVariant = 'pedidos' | 'home' | 'wallet'`.

`identity-soft-banner.component.html` línea 2 (NO se modifica):
`<div class="soft-banner" [class]="'soft-banner--' + variant" ...>` — Angular Ivy fusiona la clase estática con el binding `[class]`, así que `.soft-banner` y `.soft-banner--home` aplican juntas. El modificador SCSS funciona sin tocar el HTML.

Consumidores del componente (contexto, no se modifican):
- `src/app/pages/new/home/home.component.ts` línea 28 → `variant="home"`
- `src/app/pages/new/pedidos/ordenes/ordenes.component.ts` línea 23 → `variant="pedidos"`
- `src/app/pages/new/financiero/wallet/wallet.component.ts` línea 32 → `variant="wallet"`

Tokens disponibles en `styles/variables-new` (ya importado con `@use ... as *` en ambos SCSS): `$space-4/-5/-6/-8`, `$radius-2/-3/-4`, `$shadow-large`, `$neutral-white`, `$primary-500/-600`, `$bp-lg/-md/-sm`, `$font-primary`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regatear y reformatear el banner de Home como panel flotante (Parte A2)</name>
  <files>src/app/common/components/identity-soft-banner/identity-soft-banner.component.ts, src/app/common/components/identity-soft-banner/identity-soft-banner.component.scss</files>
  <action>
En el `.ts`, dentro del `computed` `isVisible` (líneas 27-31), añadir la condición `this.stateV2.fase0TipoUsuario() === 'activo'` junto a las dos existentes (`faseProyecto() !== 'fase0'` y `showSoftTouchpoints()`). Actualizar el comentario de bloque de arriba (líneas 25-26) para que explique la nueva regla: en Fase 1-5 el aviso de Home sigue el mismo eje Nuevo/Activo que el track de Fase 0 — un usuario nuevo no ve nada, un usuario activo (~20 órdenes, valor de trabajo de `REUCONTI.md`) sí. `stateV2` ya está inyectado; no añadir imports.

Ojo con el alcance del gating: el gate por `fase0TipoUsuario` aplica al componente entero (las tres variantes), lo cual es correcto y deseado — el eje Nuevo/Activo gobierna todos los soft touchpoints. No introducir una condición dependiente de `variant` en el `.ts`.

En el `.scss`, agregar un modificador `&--home` anidado dentro de `.soft-banner`, colocado **después** del bloque `&--pedidos` y **después** del `@media (max-width: $bp-md)` interno de `.soft-banner` — la especificidad de `.soft-banner--home` es la misma que la de `.soft-banner`, así que el orden en el archivo es lo que decide quién gana. El modificador debe replicar el footprint de `.fase0-panel` (`identity-fase0-panel.component.scss` líneas 9-32), no inventar uno nuevo:
- Sacarlo del flujo: `position: fixed`, `right: $space-6`, `bottom: $space-6`, `top: auto`, `margin-bottom: 0`, `z-index: 1200`.
- Dimensiones: `width: min(25vw, 400px)`, `min-width: 320px`, `max-width: 400px`, `max-height: calc(100vh - 53px - #{$space-8})` (53px es la altura del header, declarada como `$header-height` local en el SCSS del panel Fase 0 — replicar como variable local del mismo nombre al tope del archivo, no hardcodear el número inline).
- Composición: `flex-direction: column`, `align-items: flex-start`, `gap: $space-4`, `background: $neutral-white`, `border-radius: $radius-3`, `box-shadow: $shadow-large`, `border: none` (reemplaza el borde sutil actual), `padding: $space-5`.
- Animación de entrada slide-up: nuevo `@keyframes soft-banner-home-in` que va de `transform: translateY(20px); opacity: 0` a `translateY(0); opacity: 1`, aplicado con `animation: soft-banner-home-in 0.32s ease both`. Esto reemplaza el `fadeSlideDown` actual sólo para `--home` (que baja desde arriba y es incoherente con un panel anclado abajo). **No** borrar el keyframe `fadeSlideDown` ni la línea `animation` de la base — `pedidos` y `wallet` lo siguen usando.
- Interno: dentro de `&--home`, dar `width: 100%` a `.soft-banner__actions` con `justify-content: space-between`, para que el CTA y la X no queden apretados en la columna estrecha.
- Responsive: replicar los tres breakpoints de `.fase0-panel.scss` (líneas 198-223) como overrides específicos de `.soft-banner--home`, no globales. En `$bp-lg`: `width: min(360px, calc(100vw - #{$space-8}))`, `min-width: 0`. En `$bp-md`: `right: $space-4`, `bottom: $space-4`, `left: $space-4`, `width: auto`, `max-width: none`, `min-width: 0`, `margin: 0 auto`. En `$bp-sm`: reducir padding a `$space-4`.

Las variantes `pedidos` y `wallet` **no se tocan** — siguen siendo banners inline en sus páginas. Verificar que ninguna regla nueva quede fuera del scope `&--home`.

El copy no cambia: el texto de `home` (`'Ya tienes ganancias en camino'` / `'Para retirar tus ganancias, activa tus retiros completando tu verificación de identidad.'` / CTA `'Activar retiros'`) ya conecta ganancias con identidad y se mantiene por decisión del usuario.
  </action>
  <verify>
    <automated>yarn build 2>&1 | grep -iE "error|failed" ; echo "EXIT_MARKER"</automated>
    <human-check>Con `yarn start`, en `/new/home`: (1) Fase de entrega = cualquier Fase 1-5 + Tipo de usuario = Nuevo → sin panel ni banner de identidad en Home. (2) Mismo estado + Tipo de usuario = Activo, identidad sin completar → panel de ganancias en la esquina inferior derecha con animación slide-up, cerrable con X, sin tapar el resto de la pantalla. (3) Fase de entrega = Fase 0 + Activo → sigue apareciendo `IdentityFase0PanelComponent` sin cambios. (4) A 1024px de viewport el panel no se corta ni genera scroll horizontal (regla del proyecto). (5) `/new/pedidos/ordenes` y `/new/financiero/wallet` con usuario Activo → banners siguen inline, sin cambio visual.</human-check>
  </verify>
  <done>`yarn build` compila sin errores. `isVisible` incluye `fase0TipoUsuario() === 'activo'`. `.soft-banner--home` existe con `position: fixed` + `box-shadow: $shadow-large` + keyframe slide-up propio + los tres breakpoints. Ninguna regla de `--pedidos` ni `--wallet` fue modificada.</done>
</task>

<task type="auto">
  <name>Task 2: Copy "Completar ahora" y exponer el toggle Nuevo/Activo en todas las fases (Parte A3 + A4)</name>
  <files>src/app/common/components/identity-fase0-panel/identity-fase0-panel.component.html, src/app/common/services/identity-fase0.service.ts, src/app/layout/layout-new/demo-panel/prototype-demo-panel.component.html</files>
  <action>
**Copy (A3).** En `identity-fase0-panel.component.html` línea 49, cambiar el texto del botón de `Verificar ahora` a `Completar ahora`. En `identity-fase0.service.ts` línea 50, el mensaje CRM simulado cita textualmente ese botón (`...pulsa «Verificar ahora».`); actualizar la cita a `«Completar ahora»` para que siga describiendo el botón real. No tocar `src/app/pages/old/identidad-hub/identidad-hub.component.html` (prototipo legado, fuera de alcance).

**Toggle (A4).** En `prototype-demo-panel.component.html`, el control `TIPO DE USUARIO (HOME)` (líneas 141-155) vive hoy dentro de `@if (showFase0Controls()) { ... }` (abre en línea 140), que sólo es `true` cuando `faseProyecto() === 'fase0'`. Mover ese único `<div class="demo-control" data-tour="fase0-tipo-usuario">` **fuera** del bloque condicional, colocándolo justo antes de la línea `@if (showFase0Controls()) {`, de modo que quede a continuación del control `MOTOR` (líneas 133-137). El resto de controles exclusivos de Fase 0 (`MARCA BLANCA`, `SALDO NEGATIVO / FRAUDE`, `MOTIVO PENDIENTE`) se quedan dentro del `@if` sin cambios. Conservar el atributo `data-tour="fase0-tipo-usuario"` intacto (lo consume el tour guiado).

Renombrar la etiqueta de `TIPO DE USUARIO (HOME)` a `TIPO DE USUARIO` — ya no es un control exclusivo del Home de Fase 0.

Actualizar el texto de ayuda `.demo-panel__mecanismo` (líneas 150-154) para que refleje que el toggle ahora gobierna **ambos** tracks. Rama `'activo'`: explicar que en Fase 0 ve el Panel Lateral "Verifica tu cuenta" y en Fase 1-5 ve el panel de ganancias en la esquina inferior derecha del Home. Rama `'nuevo'`: explicar que en Home no ve ningún aviso de identidad en ninguna fase, y que sólo un clic financiero dispara el Modal Interceptor (hard gate), igual que a un Activo. Mantener el mismo tono y registro que el copy actual.

No se requiere cambio estructural en `prototype-demo-panel.component.ts`: `fase0TipoUsuario` (línea 199), `fase0TipoUsuarioOptions` (línea 200), `fase0TipoUsuarioLabel` (línea 364) y `setFase0TipoUsuario` ya son miembros del componente, no están anidados bajo `showFase0Controls` (línea 198). Verificar que siga siendo así antes de dar la tarea por cerrada; si algo estuviera anidado, exponerlo.
  </action>
  <verify>
    <automated>yarn build 2>&1 | grep -iE "error|failed" ; grep -rn "Verificar ahora" src/app/common src/app/layout src/app/pages/new | wc -l</automated>
  </verify>
  <done>`yarn build` compila sin errores. `grep -rn "Verificar ahora"` sobre `src/app/common`, `src/app/layout` y `src/app/pages/new` devuelve 0 resultados (el único remanente vive en `src/app/pages/old/`, intencional). El control `TIPO DE USUARIO` aparece antes de `@if (showFase0Controls())` y los otros tres controles de Fase 0 siguen dentro. El texto de ayuda menciona ambos tracks.</done>
</task>

<task type="auto">
  <name>Task 3: Resolver el umbral de "activo" y hacer visible el fork Nuevo/Activo en el blueprint Fase 5 (Parte B)</name>
  <files>docs/validacion/Service_Blueprint_Diagrama_Fase_5.md, docs/validacion/Service_Blueprint_Diagrama_Fase_5.html</files>
  <action>
**B1 — resolver el punto abierto.** En ambos archivos, cambiar el framing de "🚧 en discusión" a **valor de trabajo confirmado**, manteniendo la cita a `REUCONTI.md` como fuente. Ubicaciones exactas verificadas:

`Service_Blueprint_Diagrama_Fase_5.md`:
- línea 139 (tabla SI→ENTONCES de ETAPA 2): `Ya es "activo" (umbral en discusión, ronda las ~20 órdenes)` → `Ya es "activo" (20 órdenes — valor de trabajo confirmado)`.
- línea 146 (nota de punto abierto): reconvertir de `> 🚧 **Punto abierto — cuándo empieza a ver algo un usuario nuevo:**` a una nota resuelta, p.ej. `> ✅ **Regla definida — cuándo empieza a ver algo un usuario nuevo:**`. Conservar el cuerpo (la explicación de Catalina, la cita a `REUCONTI.md`, el reemplazo del disparador anterior "1ª orden o 1er movimiento de wallet", y el vínculo con la Regla del Despliegue por Lotes / Periodo Pedagógico de `Reglasvalidacion.md`), pero reescribir "aún **en discusión** con Producto" como una línea breve de cierre: el número exacto puede refinarse más adelante con Producto, pero **ya no bloquea el diseño ni la construcción**. No eliminar la nota entera.
- línea 227 (C4, línea de usuarios): `que ya es "activo" (🚧 umbral en discusión, ~20 órdenes)` → `que ya es "activo" (20 órdenes — valor de trabajo confirmado)`.

`Service_Blueprint_Diagrama_Fase_5.html`:
- línea 435 (caja C4 de la grilla): el `<span class="format-tag open">🚧 No en la 1ª venta — solo al volverse "activo" (~20 órdenes, en discusión)</span>` pasa a una clase de tag resuelto y copy `No en la 1ª venta — solo al volverse "activo" (20 órdenes, valor de trabajo)`. Usar una clase de tag ya existente en la hoja de estilos del propio archivo (revisar el bloque `<style>`, p.ej. `format-tag silent` o equivalente neutro/verde) en vez de `open`; **no** inventar una clase nueva sin declararla. Si ninguna clase existente encaja, declarar `format-tag confirmed` en el `<style>` reutilizando las variables de color ya definidas (`--f0-green-bg`, `--f0-green-text`).
- línea 544 (nota bajo la caja "¡Tu primera venta en Dropi!"): reescribir la `<em>Nota (reunión de seguimiento, REUCONTI.md)...</em>` para que exprese la regla como resuelta — el disparador **es** el umbral de activo (20 órdenes), no la 1ª venta — y ajustar el `<span class="format-tag open">🚧 Punto abierto — disparador</span>` de la línea 541 al mismo tag resuelto.
- línea 777 (tabla de segmentos): `Ya es "activo" (umbral en discusión, ~20 órdenes) y le falta algo` → `Ya es "activo" (20 órdenes — valor de trabajo confirmado) y le falta algo`.
- línea 776: el `<span class="format-tag open">🚧</span>` de la fila "Es nuevo y recién hace su 1ª orden" ya no marca un punto abierto (la regla quedó definida) — quitarlo o convertirlo al tag resuelto, dejando el texto de la fila intacto.

**No tocar** los otros `format-tag open` del archivo (reconocimiento cross-country, nivel 2 escalonado, reconciliación parcial, alcance KYC/KYB, formato del indicador persistente, catálogo de causas, canal post-suspensión, jobs de sincronización, excepción EC/CL/AR, caso Colombia) — son puntos abiertos distintos que siguen abiertos.

**B2 — fork visual en la grilla.** En `Service_Blueprint_Diagrama_Fase_5.html`, dentro de la celda de ETAPA 2 del carril Tarea (`<div class="cell etapa-multi">` que abre en línea 420), en la `sub-col` de **C4** (líneas 434-439), añadir un elemento de bifurcación visual reutilizando el mismo lenguaje del diamante de decisión que ya existe en la celda anterior (líneas 409-418): `diamond-wrap` / `diamond` / `branch-row` con `b-none` y `b-gap`, más `terminal` para el desenlace. Las clases ya están declaradas en el `<style>` del archivo (líneas 204-209 y 242) — **no** crear CSS nuevo.

Estructura del fork: diamante con la pregunta `¿Ya es "activo"?` y dos ramas rotuladas:
- rama tipo `b-none` (verde, "no pasa nada"): **Usuario nuevo → no ve nada** — no recibe aviso de identidad ni facturación todavía; sólo un clic financiero lo lleva al hard gate (C3).
- rama tipo `b-gap` (rojo, "hay algo pendiente"): **Usuario activo (20+ órdenes) → Panel Lateral en Home** — ve la bienvenida/alerta C4d, no bloqueante, puede posponer.

Anclar los conectores (`v-link` / `h-link`) a la caja, no a la celda — hay memoria registrada de un ajuste previo sobre este mismo diagrama por ese motivo. Mantener el formato de grilla por swimlanes ya establecido: **no** introducir Mermaid ni SVG nuevo, y no alterar el número de columnas ni el `grid-template` de la fila.

**B3 — cross-check de cobertura (sólo lectura, sin edición esperada).** Antes de cerrar, confirmar que el `.md` sigue reflejando los puntos con severidad ≥3 de los tres documentos de análisis (`Casos-Externos-Referencia-Fase5.md`, `Discovery-Riesgos-Transicion-Fase5.md`, `Especificaciones-UX-Mejoras-Fase5.md`). Ya está verificado para los puntos 1-9 y comentarios A-C de `Especificaciones-UX-Mejoras-Fase5.md`, y para Regla E (Bloqueado ≠ Rechazado). Los hallazgos de `Discovery-Riesgos-Transicion-Fase5.md` §8 (frentes de otros proyectos de Darwin — badge B1, Panel Antifraude, MFA) son cruces con *otros* proyectos, no especificaciones de UX del flujo propio de Dropi: **no** forzarlos a entrar al blueprint. Si el cross-check encuentra un hueco real, reportarlo en el SUMMARY sin ampliar el alcance de este pase.

Mantener `.md` y `.html` sincronizados en contenido: cada cambio de copy de B1 debe existir en ambos.
  </action>
  <verify>
    <automated>grep -c "en discusión" docs/validacion/Service_Blueprint_Diagrama_Fase_5.md ; grep -c "umbral en discusión" docs/validacion/Service_Blueprint_Diagrama_Fase_5.html ; grep -c "diamond" docs/validacion/Service_Blueprint_Diagrama_Fase_5.html</automated>
    <human-check>Abrir `docs/validacion/Service_Blueprint_Diagrama_Fase_5.html` en el navegador: (1) la grilla de ETAPA 2 muestra el fork Nuevo/Activo en la sub-columna C4, legible en una lectura rápida sin abrir el accordion; (2) los conectores del fork apuntan a las cajas, no quedan flotando en la celda; (3) el layout de swimlanes no se rompió — mismo número de columnas, sin desbordes; (4) ningún otro `🚧` del documento desapareció.</human-check>
  </verify>
  <done>Ninguna de las cuatro ubicaciones de "20 órdenes" en `.md`/`.html` presenta ya el framing "🚧 en discusión"; todas dicen valor de trabajo confirmado y conservan la referencia a `REUCONTI.md`. La nota larga del `.md` línea 146 sigue existiendo, reconvertida a regla definida con la línea de cierre sobre refinamiento futuro. El `.html` tiene un bloque `diamond-wrap` nuevo en la sub-columna C4 de ETAPA 2 usando sólo clases ya declaradas. Los `format-tag open` no relacionados con el umbral siguen intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(ninguna nueva)* | Este pase toca sólo presentación de un prototipo cliente-side (gating de visibilidad de un banner, copy, y documentación estática). No se añaden endpoints, no se procesa input de usuario, no se instalan paquetes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-bf9-01 | Information Disclosure | `identity-soft-banner` (panel `position: fixed`, `z-index: 1200`) | accept | Prototipo interno sin PII real; el contenido del panel es copy estático de marketing, no datos de cuenta. El `z-index` iguala el del panel Fase 0 existente y ambos son mutuamente excluyentes por fase. |
| T-bf9-02 | Tampering | Gating por `fase0TipoUsuario` en `localStorage` (`dropi.identityV2.fase0TipoUsuario`) | accept | Es un control de demo, no un control de seguridad. La regla real de "activo = 20 órdenes" se evalúa en backend cuando el flujo se implemente en producción; aquí sólo simula estados para stakeholders. |
| T-bf9-SC | Tampering | npm/yarn installs | n/a | Este plan no instala paquetes. No aplica el gate de legitimidad. |
</threat_model>

<verification>
- `yarn build` compila sin errores tras Task 1 y tras Task 2.
- `grep -rn "Verificar ahora" src/app/common src/app/layout src/app/pages/new` → 0 resultados.
- `grep -n "umbral en discusión" docs/validacion/Service_Blueprint_Diagrama_Fase_5.html` → 0 resultados.
- Recorrido manual en `/new/home` de las cuatro combinaciones del demo panel (Fase 0/Fase 1-5 × Nuevo/Activo) descritas en Task 1.
- Verificación a 1024px de viewport sin scroll horizontal ni clipping (regla obligatoria del proyecto).
</verification>

<success_criteria>
1. En Fase 1-5, un usuario **Nuevo** no ve ningún aviso de identidad en Home; un usuario **Activo** con identidad incompleta ve el panel de ganancias flotante en la esquina inferior derecha, con animación de entrada y cerrable con X.
2. El track Fase 0 no cambia de comportamiento: `IdentityFase0PanelComponent` sigue apareciendo igual para Activo, ahora con el botón "Completar ahora".
3. Las variantes `pedidos` y `wallet` del soft banner siguen siendo inline, sin cambio visual.
4. El toggle Nuevo/Activo está disponible en el demo panel sin importar la fase de proyecto, con texto de ayuda que describe ambos tracks.
5. El blueprint Fase 5 (`.md` + `.html`) presenta "activo = 20 órdenes" como valor de trabajo confirmado con fuente `REUCONTI.md`, y la grilla de ETAPA 2 hace visible el fork Nuevo/Activo sin necesidad de abrir el accordion.
6. `yarn build` compila sin errores.
</success_criteria>

<output>
Create `.planning/quick/260811-bf9-restaurar-diferenciacion-nuevo-activo-en/260811-bf9-SUMMARY.md` when done
</output>
