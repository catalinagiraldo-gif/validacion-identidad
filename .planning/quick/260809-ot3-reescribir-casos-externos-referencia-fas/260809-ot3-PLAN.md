---
phase: quick/260809-ot3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/validacion/Casos-Externos-Referencia-Fase5.md
autonomous: true
requirements:
  - DOC-OT3-01  # Sintesis 9.3 (3 gaps severidad 4/4) al inicio, antes de la seccion 1
  - DOC-OT3-02  # Seccion 1: filas iFAST y Wirex completadas con cita + heuristica (9.2)
  - DOC-OT3-03  # Seccion 2 (2.1-2.7) reescrita: cita exacta + heuristica + gap Fase 5 + severidad 0-4
  - DOC-OT3-04  # Las 4 correcciones de fidelidad de 9.1 aplicadas
  - DOC-OT3-05  # Secciones 3, 4 y Verificacion conservadas, con ajuste minimo derivado de 9.1

must_haves:
  truths:
    - "El documento abre con el titulo y '## Contexto y proposito' actuales sin cambios, seguidos de una sintesis con los 3 gaps de severidad 4/4, ubicada antes de '## 1. Correccion necesaria en Discovery inicial.md'"
    - "Cada uno de los 17 casos analizados en 9.2 aparece con cita textual exacta, heuristica de Nielsen/Krug, gap concreto en Fase 5 y severidad 0-4 con justificacion"
    - "Uber sigue siendo el unico caso de contraste sin severidad asignada"
    - "El documento no menciona 'biometria facial' atribuida a Nubank ni el umbral 'US$600' de eBay"
    - "eBay y Twitch estan marcados como 'no confirmado verbatim - pendiente de verificacion manual con navegador'"
    - "Las secciones 3 (tabla de fuerza de evidencia), 4 (Conclusion) y 'Verificacion' siguen presentes"
    - "El documento no tiene front-matter YAML"
  artifacts:
    - path: "docs/validacion/Casos-Externos-Referencia-Fase5.md"
      provides: "Casos externos enriquecidos con cita textual, heuristica, gap y severidad por caso"
      min_lines: 120
      contains: "Severidad: 4"
  key_links:
    - from: "docs/validacion/Casos-Externos-Referencia-Fase5.md"
      to: "9.2 del plan aprobado (los-riesgos-la-experiencia-cryptic-mccarthy.md)"
      via: "transcripcion fiel de cita/heuristica/gap/severidad por caso"
      pattern: "Severidad: [0-4]"
    - from: "docs/validacion/Casos-Externos-Referencia-Fase5.md"
      to: "9.3 del plan aprobado"
      via: "seccion de sintesis con los 3 gaps 4/4 al inicio"
      pattern: "4/4"
---

<objective>
Reescribir `docs/validacion/Casos-Externos-Referencia-Fase5.md` para hacerlo especifico por caso: cita textual exacta de cada fuente, heuristica de Nielsen/Krug aplicable, gap concreto en Fase 5 y severidad 0-4 con justificacion.

Purpose: el documento actual resume los casos pero no permite que Producto defienda ante stakeholders *que dice literalmente cada fuente* ni *que le falta a Fase 5 por cada caso*. El contenido enriquecido ya esta redactado y aprobado por el usuario en las secciones 9.1, 9.2 y 9.3 del plan fuente.

Output: `docs/validacion/Casos-Externos-Referencia-Fase5.md` reescrito. Es una transcripcion fiel del plan aprobado, no contenido nuevo.
</objective>

<execution_context>
@D:/validacion-identidad/.claude/get-shit-done/workflows/execute-plan.md
@D:/validacion-identidad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/validacion/Casos-Externos-Referencia-Fase5.md

**Fuente unica de contenido (obligatorio leer antes de escribir):**
`C:\Users\USER\.claude\plans\los-riesgos-la-experiencia-cryptic-mccarthy.md`, lineas ~276-357 — secciones `### 9.1`, `### 9.2` y `### 9.3`.

Leer ese rango con `Read(offset=270, limit=130)`. **Una sola lectura.** Todo el contenido a transcribir esta ahi.

**Regla de oro de este plan:** NO inventar, NO reinterpretar, NO agregar casos, NO agregar heuristicas ni severidades que no esten en 9.2. Si algo no esta en el plan fuente, no va en el documento.

**Herramientas:** el Bash de este equipo falla con errores de `fork` (`dofork: child -1 ... Resource temporarily unavailable`). Usar el tool `Write` para escribir el archivo y el tool `Grep` (o PowerShell `Select-String`) para verificar. No usar heredoc ni `cat`. Los comandos de `<verify>` estan escritos como PowerShell directo (shell primario de este equipo), no como `powershell -Command "..."` anidado.

**Estilo:** narrativo, en espanol, igual al resto de `docs/validacion/`. Sin front-matter YAML. Sin emojis. Mantener el uso de negritas para nombres de caso y de *cursiva* para citas textuales, como ya lo hace 9.2.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir el documento con sintesis 9.3, seccion 1 completada y seccion 2 enriquecida</name>
  <files>docs/validacion/Casos-Externos-Referencia-Fase5.md</files>
  <action>
Leer primero el rango 9.1/9.2/9.3 del plan fuente (`Read` con offset=270, limit=130 sobre `C:\Users\USER\.claude\plans\los-riesgos-la-experiencia-cryptic-mccarthy.md`). Luego reescribir el documento completo con `Write`, respetando esta estructura:

1. **Titulo y `## Contexto y proposito`**: copiar tal cual del documento actual, sin cambios.

2. **Nueva sub-seccion de sintesis**, insertada despues de "Contexto y proposito" y ANTES de `## 1. Correccion necesaria en Discovery inicial.md`. Titulo sugerido: `## Sintesis — los 3 gaps de experiencia mas severos (4/4)`. Contenido: los 3 items numerados de 9.3, transcritos literalmente (step-up/reautenticacion contextual post-aprobacion; identidad reutilizable entre modulos/paises/entidades; senal de vinculo entre cuentas para prevenir evasion de baneo), con los casos que los respaldan entre parentesis como en 9.3. Agregar, antes de la lista, una linea con la convencion de severidad de 9.2: `0=no es problema · 1=cosmetico · 2=menor (retraso/frustracion) · 3=mayor (falla significativa de tarea) · 4=catastrofico (impide completar la tarea)`.

3. **Seccion 1 (tabla de correccion de `Discovery inicial.md`)**: mantener la tabla y sus 5 filas. Completar las filas de **iFAST Global Bank** y **Wirex** agregando su cita textual + heuristica tomadas de la sub-seccion "Seccion 1 (casos ya citados)" de 9.2. Si la cita+heuristica+gap+severidad no caben legiblemente en la celda de tabla, dejar la tabla como esta y agregar inmediatamente debajo dos bloques narrativos (`**iFAST Global Bank + Sumsub** — Cita: ... Heuristica: ... **Gap en Fase 5:** ... **Severidad: 3**`) con el texto integro de 9.2. Ambos casos deben terminar con `Severidad: 3`.

4. **Seccion 2 reescrita completa (2.1 a 2.7)**: conservar los 7 encabezados de sub-seccion actuales y el orden actual de casos. Cada caso pasa de bullet-resumen a bloque con los cuatro elementos de 9.2, en este orden: cita textual exacta (en cursiva, entre comillas), heuristica de Nielsen/Krug aplicable, `**Gap en Fase 5:**` con el texto especifico, y `**Severidad: N**` con su justificacion entre parentesis cuando 9.2 la trae. Conservar la URL de fuente que ya tiene cada caso en el documento actual. Casos y severidades esperadas (todas de 9.2, no derivar ninguna):
   - 2.1: Stripe Connect (3), eBay (2), Airbnb reporte fiscal (2), Uber — caso de contraste, **sin severidad**, se mantiene como esta.
   - 2.2: CS.Money + Sumsub (2), Binance tiers (3).
   - 2.3: Consensys + Trulioo (4), Twitch (2).
   - 2.4: Rappi + Jumio (3) — incluir explicitamente el parrafo de 9.2 que dice lo que Fase 5 YA hace bien (gate por accion, no por navegacion) antes del gap real. Conservar tambien el bullet actual sobre Mercado Pago/Nubank/Wise/dLocal/etc. sin case study publico.
   - 2.5: Triple-A + Elliptic (3), Chainalysis KYT + Authsignal (4). Conservar el bullet actual sobre la ausencia de caso "marketplace LATAM + retiro cripto + KYT".
   - 2.6: Revolut (4), Nubank Defense Platform (3, con la cita CORREGIDA, ver punto 5a).
   - 2.7: Sumsub Reusable KYC PayDo/Mercuryo (4), Industry Sharing Safety Program (3), Airbnb "closely associated" (4).

5. **Aplicar las 4 correcciones de 9.1**:
   a. **Nubank**: eliminar toda mencion de "biometria facial ... activo por default" (no respaldada por la fuente). Sustituir por la distincion sincrono/asincrono confirmada: acciones en tiempo real y visibles ("blocking the transaction or displaying a warning in the app") vs. asincronas e invisibles ("opening an internal case for investigation").
   b. **eBay**: reemplazar el umbral "US$600 en ventas/ano" por "gross payments exceeding $20,000 AND more than 200 transactions" (vigente 2025-2026), y aclarar que es un umbral que cambia por regulacion externa, no un valor fijo de diseno. Escribir la cifra tambien en formato local (`US$20.000 + 200 transacciones`) para que el grep de verificacion la encuentre.
   c. **eBay y Twitch (1800accountant.com)**: marcar ambos con el inciso literal `*(fuente no re-verificable verbatim — pendiente de verificacion manual con navegador)*` junto al nombre del caso.
   d. **Airbnb (reporte fiscal)**: aclarar que la fuente es el articulo `US income tax reporting overview for hosts`, especifico sobre reporte fiscal en EE.UU., no un articulo general de identidad.
   Ademas, agregar una sub-seccion corta que liste las 4 correcciones de fidelidad (texto de 9.1), para que el lector entienda por que cambiaron esas cifras respecto de la version anterior del documento. Ubicarla dentro de la seccion 1 o inmediatamente despues, a criterio de legibilidad.

6. **Secciones 3, 4 y `## Verificacion`**: copiar del documento actual en esta misma escritura (se ajustan en Task 2). No borrarlas.
  </action>
  <verify>
    <automated>$p='docs/validacion/Casos-Externos-Referencia-Fase5.md'; $sev=(Select-String -Path $p -Pattern 'Severidad: [0-4]' -AllMatches).Matches.Count; $her=(Select-String -Path $p -Pattern 'Heur' -AllMatches).Matches.Count; "sev=$sev heur=$her"; if ($sev -lt 17 -or $her -lt 17) { exit 1 }</automated>
  </verify>
  <done>El archivo contiene 17 o mas ocurrencias de `Severidad: N` (una por caso analizado, Uber excluido) y 17 o mas menciones de heuristica; la sintesis de los 3 gaps 4/4 aparece antes de `## 1.`; secciones 3, 4 y Verificacion siguen presentes.</done>
</task>

<task type="auto">
  <name>Task 2: Auditar fidelidad de las 4 correcciones y ajustar la tabla resumen</name>
  <files>docs/validacion/Casos-Externos-Referencia-Fase5.md</files>
  <action>
Auditoria de fidelidad sobre el archivo ya escrito, con `Grep` sobre el documento (no reabrir el plan fuente si ya esta en contexto):

1. Confirmar que NO aparece `biometria facial` / `biometria facial` asociada a Nubank, y que NO aparece `US$600` ni `$600`.
2. Confirmar que aparece `20.000` (o `$20,000`) junto a `200 transacciones` en el caso eBay.
3. Confirmar que la marca `verbatim` aparece al menos dos veces (eBay y Twitch).
4. Confirmar que aparece la frase `US income tax reporting overview for hosts`.
5. Confirmar que hay exactamente 5 casos con `Severidad: 4` (Consensys+Trulioo, Chainalysis+Authsignal, Revolut, Sumsub Reusable KYC, Airbnb "closely associated") y que Uber no tiene severidad asignada.

Luego, ajuste minimo de la **seccion 3 (tabla resumen de fuerza de evidencia)** derivado exclusivamente de las correcciones de 9.1:
- Fila **eBay**: la fuerza de evidencia ya no puede ser "Alta — help center oficial" sin matiz; ajustarla para reflejar que la cita no fue confirmada verbatim (ej. "Media — patron documentado, cita no confirmada verbatim; umbral federal 2025-2026: US$20.000 + 200 transacciones").
- Fila **Nubank Defense Platform**: verificar que su descripcion no dependa de la afirmacion eliminada sobre biometria facial; si la menciona, corregirla a la distincion sincrono/asincrono.
- No agregar filas nuevas, no cambiar el resto de las filas, no cambiar la conclusion de la seccion 4 salvo que contradiga alguna de las 4 correcciones (revisar; hoy no las menciona, asi que lo esperable es dejarla intacta).
- Dejar `## Verificacion` tal cual esta en el documento actual.

Si alguna comprobacion de la auditoria falla, corregir el documento reescribiendolo con `Write` (el tool `Edit` puede no estar disponible) — sin agregar contenido que no este en 9.1/9.2/9.3.
  </action>
  <verify>
    <automated>$p='docs/validacion/Casos-Externos-Referencia-Fase5.md'; $bad=(Select-String -Path $p -Pattern 'biometr.a facial|US\$600' -AllMatches).Matches.Count; $ebay=(Select-String -Path $p -Pattern '200 transacciones' -AllMatches).Matches.Count; $verb=(Select-String -Path $p -Pattern 'verbatim' -AllMatches).Matches.Count; $src=(Select-String -Path $p -Pattern 'US income tax reporting overview for hosts' -AllMatches).Matches.Count; $s4=(Select-String -Path $p -Pattern 'Severidad: 4' -AllMatches).Matches.Count; "bad=$bad ebay=$ebay verbatim=$verb hosts=$src sev4=$s4"; if ($bad -ne 0 -or $ebay -lt 1 -or $verb -lt 2 -or $src -lt 1 -or $s4 -ne 5) { exit 1 }</automated>
  </verify>
  <done>`bad=0`, `ebay>=1`, `verbatim>=2`, `hosts>=1`, `sev4=5`. La fila de eBay en la tabla resumen refleja el matiz de no-confirmado-verbatim y el umbral corregido. Secciones 4 y Verificacion intactas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan fuente -> documento para stakeholders | Contenido que Producto llevara a una reunion; una cita inventada o mal atribuida es riesgo reputacional real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ot3-01 | Tampering | Citas textuales del documento | mitigate | Transcripcion literal desde 9.2; prohibido parafrasear o reinterpretar citas; Task 2 audita fidelidad con checks automatizados |
| T-ot3-02 | Repudiation | Cifras no verificables (eBay, Twitch) | mitigate | Marca explicita "fuente no re-verificable verbatim — pendiente de verificacion manual con navegador", verificada por grep (>=2 ocurrencias) |
| T-ot3-03 | Information Disclosure | Datos internos de Dropi en el documento | accept | El documento ya vive en `docs/validacion/` (interno); no se agrega informacion nueva mas alla de lo aprobado en el plan |
| T-ot3-SC | Tampering | npm/pip/cargo installs | N/A | Cambio solo de documentacion, sin instalacion de paquetes |
</threat_model>

<verification>
- El documento no tiene front-matter YAML (la primera linea es `# Casos externos de referencia...`).
- Los 7 encabezados de sub-seccion de la seccion 2 (2.1 a 2.7) siguen presentes y en el mismo orden.
- Ninguna URL del documento actual se perdio en la reescritura.
- Ningun caso nuevo fue agregado mas alla de los que ya existian en el documento y en 9.2.
- Solo se modifico `docs/validacion/Casos-Externos-Referencia-Fase5.md`. No se toco `Discovery-Riesgos-Transicion-Fase5.md`, `Discovery inicial.md` ni ningun archivo de codigo.
</verification>

<success_criteria>
- La sintesis de los 3 gaps 4/4 aparece antes de la seccion 1, con la convencion de severidad explicada.
- 17 casos con cita textual + heuristica + gap en Fase 5 + severidad; Uber conservado como contraste sin severidad.
- Las 4 correcciones de 9.1 aplicadas y verificables por grep.
- Secciones 3, 4 y Verificacion conservadas, con el unico ajuste de la fila eBay (y Nubank si aplicaba).
- Cero contenido fuera de 9.1/9.2/9.3 del plan aprobado.
</success_criteria>

<output>
Crear `.planning/quick/260809-ot3-reescribir-casos-externos-referencia-fas/260809-ot3-SUMMARY.md` al terminar.

Nota: el registro en `.planning/STATE.md` y el commit los maneja el flujo `/gsd-quick`, no las tareas de este plan (el commit debe hacerse via PowerShell — el Bash de este equipo falla con errores de fork).
</output>
